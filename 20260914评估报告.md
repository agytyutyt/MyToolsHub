# JZToolsHub 评估报告（20260914）

| 文档属性 | 内容 |
| --- | --- |
| 评估日期 | 2026-09-14 |
| 评估基线 | 分支 `main`，HEAD `ca69141`（工作区干净，与 `origin/main` 一致） |
| 评估范围 | 全部源代码、目录结构、配置文件、16 个插件、构建与安装脚本、文档体系 |
| 评估方法 | 静态代码审阅 + 目录/清单比对 + 规范条款逐项对照 + git 历史核查 |
| 关联文档 | `README.md`、`HANDOFF.md`、`插件设计规范.md`（v1.1） |

---

## 摘要

**整体结论：架构设计扎实，插件化落地程度高，"一切皆插件"的承诺在运行时是真的成立的。** 16 个插件全部具备 `manifest.json` 与自包含目录；11 个带后端的插件 100% 实现了 `register(app)`、统一 `/api/<id>/` 前缀、依赖声明与缺库降级；所有用户数据写入均经 `jztools_data` 定位到数据根目录，未发现插件越界写入其他目录或框架目录的情况。

**主要短板不在架构，而在"规范滞后于实现"：** 《插件设计规范》v1.1（2026-08-26）成文于数据根目录改造之前，全文未出现"数据根目录"概念，仍要求数据落在 `plugins/<id>/backend/data/`；同时缺失 `home_card()` 钩子、`grant_all` 字段、endpoint 命名前缀、程序化接口、模板同步清单登记等在实战中已被证明必需的条款。规范与实现脱节，导致新人按规范写出来的插件与现有 16 个插件的实际写法不一致。

**共发现 26 项待改进项**（P0 五条、P1 六条、P2 十五条），其中 4 项会导致功能实际失效（配置模板同步链路断裂、数据目录指针入库、打包脚本误删模板、发包遗漏最新改动）。

---

## 一、评估范围与方法

| 维度 | 检查内容 |
| --- | --- |
| 目录与清单 | 16 个插件的目录结构、`manifest.json` 字段完整性、`config/tools.json` 登记一致性 |
| 后端规范 | `register(app)` 导出、路由前缀、endpoint 唯一性、依赖声明与降级、异步任务模式、数据归属与可见性过滤 |
| 前端规范 | 相对路径引用、`?v=` 版本号纪律、XSS 转义、敏感配置外置、emoji/SVG 回退 |
| 数据与配置 | 数据根目录使用、密钥 gitignore、配置模板同步清单一致性 |
| 生命周期 | 开发脚手架完整度、升级便利性、停用/卸载残留 |
| 工程与文档 | 构建/安装脚本、仓库卫生、文档与实现的一致性 |

---

## 二、插件规范符合性总览

图例：✅ 符合　⚠️ 部分符合/有瑕疵　❌ 不符合　— 不适用

| 插件 | 形态 | manifest | 目录自包含 | 路由前缀 | endpoint 前缀 | `/status` 自检 | 依赖声明 | 数据经 `jztools_data` | 归属+过滤 | 异步化 | README |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| admin | 核心 | ✅ | ✅ | ⚠️ 注1 | ⚠️ 注2 | — | ✅ | ✅ | ✅ | — | ✅ |
| notice-board | L2 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | — | ✅ |
| knowledge-base | L2 | ✅ | ⚠️ 注3 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅（按需渲染） | ✅ |
| file-filter | L2 | ✅ | ✅ | ✅ | ❌ 注4 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| trajectory-sketch | L2 | ✅ | ⚠️ 注5 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| shared-docs | L2 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | — | ✅ |
| case-report | L2 | ⚠️ 注6 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| character-graph | L2 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| trajectory-convert | L2 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| qr-video-decode | L2 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | — 纯计算 | — | ✅ | ✅ |
| info-transfer | L2 | ⚠️ 注7 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | — | ✅ | ✅ |
| map-marker | L1 | ✅ | ❌ 注8 | — | — | — | — | — | — | — | ✅ |
| base64 | L1 | ✅ | ✅ | — | — | — | — | — | — | — | ✅ |
| json-formatter | L1 | ✅ | ✅ | — | — | — | — | — | — | — | ✅ |
| color-picker | L1 | ✅ | ✅ | — | — | — | — | — | — | — | ✅ |
| md5-generator | L1 | ✅ | ✅ | — | — | — | — | — | — | — | ✅ |

**注1**：admin 除 `/api/admin/*` 外还注册了 `/login`、`/admin`、`/admin/<module>`、`/api/login` 等非 `/api/<id>/` 路由。作为核心基础设施插件这是必要的（登录页需要干净 URL），但规范 B-2 未为此开豁免口。
**注2**：`account_change_password` 未带 `admin_` 前缀（当前不冲突，但不符合约定）。
**注3**：`backend/` 内混入开发期脚本 `_build_phase8.py`、`test_routes_preview.py`。
**注4**：`def status()` 未带插件前缀——这是当前**唯一**具有高冲突风险的 endpoint 命名。
**注5**：`frontend/icons/_generate.py` 为图标生成脚本，属构建期产物。
**注6**：`manifest.features` 含"规则/大模型"，但规则解析已取消，现为仅大模型。
**注7**：`manifest.features` 含"文档提取"，v2 起主推 `fmt=file` 原文件完整传输，表述偏旧。
**注8**：`frontend/` 内混放 `generate_qr_stream.py`、`simulate_yunfu_5min.py`、`yunfu*.json/txt/mp4` 等演示与生成脚本。

### 2.1 共性优点（值得保持）

1. **目录自包含**：16 个插件全部具备 `manifest.json` + `frontend/`，11 个带后端插件全部具备 `backend/{__init__.py,routes.py}`，无一依赖框架目录或其他插件目录。
2. **数据隔离彻底**：全部后端插件通过 `jztools_data.get_data_root_dir/file()` 定位数据，未发现任何插件拼接绝对路径或写入 `static/`、`config/`、其他插件目录。符合 S-1。
3. **依赖声明与优雅降级**：每个带后端插件都有 `requirements.txt`，并在 `/status` 中逐项上报可用性；缺库时功能降级并给出提示，不 500、不拖垮主进程。符合 B-4。
4. **异步任务模式统一**：6 个长耗时插件（case-report、character-graph、trajectory-convert、qr-video-decode、file-filter、trajectory-sketch）均采用 `ThreadPoolExecutor(2)` + TTL 30 分钟 + 任务归属校验，前端轮询。符合 8.5。
5. **无 B 型插件落地**——规范第 8.6~8.10 章（jz-runtime-1 协议）目前无人使用，属"预置未验证"能力。

---

## 三、开发 / 更新 / 移除 便利性评估

### 3.1 维度与评分

| 维度 | 说明 | 平均表现 |
| --- | --- | --- |
| **开发便利** | 脚手架完整度、最小模板可用性、依赖与配置外置、调试与自检手段 | 良（7.5/10） |
| **更新便利** | 前端版本号纪律、后端幂等迁移、配置模板同步、引擎/算法可插拔 | 中（6/10） |
| **移除便利** | 登记单点化、数据单一目录、无跨插件 import、无框架残留 | 优（9/10） |

### 3.2 开发便利性

**做得好的**

- 最小模板齐全（规范附录 A/B/D），纯前端插件两个文件 + 一行配置即可上线，刷新即生效。
- 所有插件都自带 `README.md`（16/16），功能、依赖、接口、限制齐全。
- 配置外置到位：LLM/API Key 一律走 `backend/config.json`（数据根）或前端配置面板，仓库未发现硬编码密钥。
- 调试手段可用：`/api/<id>/status` 逐项报依赖；trajectory-sketch 的 `backend/engine/selftest.py` 可脱离 Flask 独立运行。

**待改进**

| # | 问题 | 影响 | 建议 |
| --- | --- | --- | --- |
| D-1 | 规范 §9.1 要求数据写 `plugins/<id>/backend/data/`，实现实际写 `<数据根>/plugins/<id>/data/` | 新人按规范写会放错位置，且打包清理逻辑（`build-deploy.ps1` 删 `data/`）只认插件树内 | 规范改为"必须经 `jztools_data` 定位"，并说明数据根布局 |
| D-2 | 规范未规定 endpoint 命名前缀，仅 HANDOFF 有记载 | 新插件仍可能写出 `def status()` 而撞车（file-filter 就是活样本） | 规范新增「B-22：路由函数必须带插件前缀」 |
| D-3 | 规范无"前端 SVG 回退图标登记"约定，且 `app.py::_EMOJI_ICON_FILES` 为硬编码表（14 条）且**前端并不消费** | 新增插件 emoji 无法自动回退（file-filter 🧹 全无回退）；维护两处映射表 | 统一到 `static/js/jz-icon.js` 一处，并改为按 codepoint 自动推导文件名 |
| D-4 | 规范未提及"程序化接口"（如 `file-filter` 的 `/apply`） | 插件间能力复用缺少标准做法，容易退回 import 对方模块 | 规范新增「SHOULD 提供 `/apply` 形式 JSON 程序化接口」 |
| D-5 | 规范未禁止测试/构建脚本随包分发（S-4 只禁 `__pycache__` 等） | `knowledge-base/backend/{_build_phase8.py,test_routes_preview.py}` 等进入部署包 | S-4 扩展：禁止 `test_*.py`、`_*build*.py`、生成器脚本随包分发 |

### 3.3 更新便利性

| # | 问题 | 影响 | 建议 |
| --- | --- | --- | --- |
| U-1 | **配置模板同步链路断裂**：`_TEMPLATE_SYNC` 与 `install.ps1` 均登记 `case-report`/`character-graph` 的 `prompt.json` 模板，但仓库中这两个文件已不存在 | 每次版本升级打印 2 条「缺少模板」告警；两插件只能用 `llm_client` 内置默认提示词，**无法通过发版更新提示词** | 二选一：补回模板文件（推荐，恢复提示词可运维能力），或从两处清单移除 |
| U-2 | **打包脚本误删同步模板**：`build-deploy.ps1` 执行 `Remove-Item` 删除插件树内所有 `config.json`，包括作为同步模板的 `trajectory-sketch/backend/config.json` | 目标机该模板永远缺失，新增配置键不会随版本下发 | 清理清单加白名单（`trajectory-sketch/backend/config.json`），或把模板改名为 `config.template.json` |
| U-3 | **最新改动未进发布包**：最近一次明确记录的打包是 v1.6（2026-09-11），而知识库 Office 引擎改造提交（`77e0f22`/`98dabe7`/`ca69141`）在其之后 | 目标机拿到的不是最新功能 | 发版前重新打包并递增 `-Version`；建立"发包前核对 HEAD 与 version.json"的检查项 |
| U-4 | **前端版本号纪律不统一**：case-report 用序号（v34）、character-graph 用日期戳（`?v=20260827`）、admin 各页对同一份 `admin.css` 引用了 v1/v2/v5 三个版本 | 同一份资源存在多个缓存副本，用户可能看到不一致的界面；改完容易漏改某页 | 统一为"每插件一个递增整数，页面内同源资源版本号必须一致"；`admin` 页面抽公共头部模板 |
| U-5 | **改引擎后缓存失效需人工介入**：知识库预览缓存 `<id>.preview.json` 无失效逻辑（设计上文件不可变） | 升级 vendor 或修引擎后，用户仍看到修复前的旧产物 | 在 `/status` 暴露引擎版本；提供「清空预览缓存」管理按钮或升级时按引擎版本自动失效 |
| U-6 | Python 版本口径不一致：README 称目标环境 3.8，知识库引擎要求 ≥3.10，打包用 3.14 | 低版本环境下知识库预览静默降级，排查困难 | README/规范统一声明"完整功能 ≥3.10"，并在 `/status` 中提示 Python 版本约束 |

### 3.4 移除便利性

**表现最优的一环。** 原因：

1. **登记单点化**：插件的一切对外身份由 `config/tools.json` 一条记录 + 一个目录构成，删条目即下线（`enabled:false` 可先软下线）。
2. **数据单一目录**：全部数据在 `<数据根>/plugins/<id>/`，删除即清理干净。
3. **无跨插件 import**：全项目无插件间模块级依赖；唯一的能力复用是 `trajectory-sketch → file-filter` 的 HTTP `/apply` 调用，符合 B-7。唯一例外是全部插件对 `jztools_admin.routes` 的 `get_session_user`/`set_operation`（规范允许的）。
4. **无框架残留**：框架不维护插件注册表以外的任何插件状态，`_plugin_home_card_hooks` 每次启动重建。

**唯一风险**：若插件曾被其他插件复用（如 `file-filter` 被 `trajectory-sketch` 依赖），直接移除会导致依赖方 `/status` 报"过滤器插件不可用"。规范未要求声明插件间依赖。建议新增可选字段 `depends_on: ["file-filter"]`，由框架在启动时校验并提示。

---

## 四、《插件设计规范》可优化之处

> 当前版本 v1.1（2026-08-26）。以下建议按"影响面"排序。

### 4.1 高优先级（规范与实现已脱节，会误导开发者）

| 编号 | 现状 | 建议 |
| --- | --- | --- |
| **N-1** | 第 9 章（数据持久化）与 4.1/4.2/S-3 均要求数据落在 `plugins/<id>/backend/data/`；实现已全面改为 `<数据根>/plugins/<id>/data/` + `.task_cache/` | 新增「数据根目录」章节：说明双指针解析、`get_data_root_dir/file()` 用法、数据根布局、`.admin_key` 与密文必须同迁；把 9.1 改为"必须经 `jztools_data` 定位，禁止拼绝对路径" |
| **N-2** | 6.2 工具条目字段表缺少 `grant_all`；README 有 | 补入 `grant_all`（`true` 时对全体登录用户开放），并说明与"按人授权"的关系 |
| **N-3** | 全文未提 `home_card()` 钩子（首页卡片动态声明，notice-board 已用） | 新增条款：钩子签名 `home_card(app=None) -> dict`，可返回 `name/description/icon/accent/features`，优先级高于 tools.json 与 manifest；异常不得阻断插件加载 |
| **N-4** | 无 endpoint 命名/冲突条款（B-2 只约束 URL 前缀） | 新增「B-22 必须：路由函数命名必须带插件 ID 前缀（如 `kb_status`）或显式传 `endpoint="<id>_<动作>"`」——Flask 以 `__name__` 作为 endpoint，重名会在启动阶段 `AssertionError` 导致插件整体加载失败 |
| **N-5** | 无"配置模板同步清单登记"要求；该机制在 `jztools_data._TEMPLATE_SYNC` 与 `install.ps1` 两处维护 | 新增条款：插件若提供随版本演进的配置模板（`prompt.json`/`config.json`），必须在上述两处同时登记并保持一致，且声明同步模式（`overwrite`/`ensure-keys`） |

### 4.2 中优先级（实战经验未沉淀）

| 编号 | 建议新增内容 |
| --- | --- |
| **N-6** | **插件间依赖声明**：新增可选清单字段 `depends_on: [插件id]`，框架启动时校验依赖是否启用，缺失时告警；复用能力只走程序化 HTTP 接口（参照 `file-filter` 的 `/apply`） |
| **N-7** | **随包分发白/黑名单**：S-4 扩展为禁止 `test_*.py`、`_build_*.py`、`*_generate.py`、演示数据、视频/大文件随插件分发；大资源应放插件 `README` 指引外链 |
| **N-8** | **前端版本号纪律细则**：F-2 补充"同源资源在所有页面中版本号必须一致；版本号统一为递增整数，起点 `?v=1`；新插件从首个外部资源起就必须带版本号" |
| **N-9** | **图标回退登记**：新增条款说明 SVG 回退机制（`static/icons/` + `jz-icon.js`），要求新增插件在登记表里补条目，或由框架改为按 emoji codepoint 自动推导文件名 |
| **N-10** | **缓存失效约定**：插件若有磁盘缓存（如知识库 `<id>.preview.json`），必须在 `/status` 暴露引擎/缓存版本号，并提供"随引擎版本自动失效"或管理端清理入口 |
| **N-11** | **Python 版本声明**：V-4 扩展为"插件必须在 README 与 manifest 附近显式声明 Python 最低版本；高于框架基线时必须在 `/status` 中提示降级原因" |

### 4.3 低优先级（严谨性与可维护性）

| 编号 | 建议 |
| --- | --- |
| **N-12** | 规范中"**必须/禁止/应当/可以**"的用法整体偏宽松（如 B-1 的 `register` 缺失是"静默不加载"）。建议框架侧增加启动自检：缺失 `register` 时告警而非静默 |
| **N-13** | 第 14 章自检清单中「用户数据只写 `backend/data/`」等条目随 N-1 同步修正；建议补充"启动日志确认出现『已注册后端插件：\<id\>』" |
| **N-14** | 附录 C 反模式清单补充三条：① 路由函数不带前缀导致 endpoint 冲突；② 把测试/构建脚本放进插件包；③ 同一份静态资源在不同页面用不同 `?v=` |
| **N-15** | B 型（封装型）插件规范第 8.6~8.10 章**无任何落地实现**，建议标注为"预置能力，未经生产验证"，或补充一个最小可运行示例以便验证协议 |
| **N-16** | 规范正文未说明"程序目录的 `config/tools.json` 只是模板，运行时读数据根目录副本"——这是排查"改了配置不生效"的高频原因，应在第 6 章显著位置说明 |

---

## 五、项目级其他问题

### 5.1 会导致功能实际失效（P0）

| # | 问题 | 证据 | 建议 |
| --- | --- | --- | --- |
| **P0-1** | 配置模板同步链路断裂：`prompt.json` 模板文件已不存在 | `jztools_data.py:73-76` 与 `install.ps1:125-126` 登记了 `case-report`/`character-graph` 的 `prompt.json`；`plugins/**/prompt.json` glob 结果为空 | 补回模板或从两处清单移除 |
| **P0-2** | 打包脚本误删同步模板 | `build-deploy.ps1:67-69` 删除插件树内所有 `config.json`，命中作为模板的 `trajectory-sketch/backend/config.json` | 加白名单或改模板文件名 |
| **P0-3** | `config/data_root.json`（含开发机绝对路径 `C:\Users\yfjz\.jztoolshub`）已纳入版本库 | `git ls-files config` 显示其被跟踪 | 加入 `.gitignore` 并 `git rm --cached`；或改为示例文件 `data_root.example.json` |
| **P0-4** | 最新功能未进任何发布包 | HEAD `ca69141`（09-14）晚于 v1.6 打包（09-11） | 发版前重新打包并递增 `-Version` |
| **P0-5** | `file-filter` 的 `def status()` 无插件前缀 | `plugins/file-filter/backend/routes.py:270` | 改名 `ff_status`（当前唯一 high-risk endpoint） |

### 5.2 工程质量（P1）

| # | 问题 | 建议 |
| --- | --- | --- |
| P1-1 | `app.py::_EMOJI_ICON_FILES`（14 条硬编码）+ `/api/tools` 返回的 `icon_file` 字段**前端无人消费**（`static/js` 中无 `icon_file` 引用），实际回退全靠 `static/js/jz-icon.js` | 二选一：删除服务端映射与字段，或让前端改消费 `icon_file`；同时为 `file-filter`（🧹）补 SVG |
| P1-2 | 前端版本号混乱（见 U-4） | 统一纪律并修正 admin 各页 |
| P1-3 | 开发/测试脚本随包分发：`knowledge-base/backend/{_build_phase8.py,test_routes_preview.py}`、`trajectory-sketch/frontend/icons/_generate.py`、`map-marker/frontend/*.py` | 移出插件目录或加入打包清理清单 |
| P1-4 | `map-marker/frontend/` 内含演示视频 mp4 与大 JSON（`yunfu-1000points.json` 等），虽已 gitignore 但会进部署包 | 同上；插件包应保持精简 |
| P1-5 | 首页会显示空分类：`ai`/`design`/`maps` 三个分类在当前默认配置下无启用工具 | 前端过滤空分类，或在 `tools.json` 中把空分类 `enabled: false` |
| P1-6 | `README.md` 中 `GET /api/shared-docs/status` 重复列出两行（旧版遗留） | 已在本次重写中修复 |

### 5.3 仓库与构建卫生（P2）

| # | 问题 | 建议 |
| --- | --- | --- |
| P2-1 | 工作区构建产物约 **1.2GB**：`deploy/`（3 个历史版本目录 236+235+240MB，11 个旧 zip）、`dist/` 220MB、`build/` 42MB，均 gitignore 但占磁盘与备份 | 只保留最近 1~2 个版本目录与 zip，其余归档到外部存储 |
| P2-2 | 仓库根目录无 `version.json`（仅部署包有），源码开发时 `sync_templates()` 直接返回 0 | 属预期行为，但应在 README 明确说明；或提供 `-Version` 生成到根目录便于开发验证 |
| P2-3 | 分支较多（`G2改造`、`PluginDesign`、`共享文档`、`功能优化`、`压力测试`、`战果录入`、`插件位置编辑`、`界面滑块`、`登录改造`）且长期未合并/清理 | 确认价值后合并或打 tag 归档删除 |
| P2-4 | 根目录 `openh264-2.5.0-win64.dll` 为 OpenCV 视频编码依赖，来源与许可证未说明 | 在 README 或 `docs/` 补充第三方组件与许可证清单（规范 S-8 对 B 型有此要求，主项目同样适用） |
| P2-5 | 无自动化测试体系：测试脚本散落在临时目录（`%TEMP%`）与插件内（`test_routes_preview.py`），未纳入 CI | 建立 `tests/` 目录，把隔离数据根目录的打桩手法固化为公共 fixture |
| P2-6 | 生产并发上限约 300（单进程线程模型） | 高负载场景评估 Gunicorn 多进程 + Nginx 反代 |

### 5.4 文档与实现一致性（P2）

| # | 问题 | 建议 |
| --- | --- | --- |
| P2-7 | 交接文档滞后于仓库：旧 HANDOFF 记录"最新提交 7269cb8、大量未提交改动"，实际 HEAD 为 `ca69141` 且工作区干净；"android-app 未纳入版本管理"也已过时（实际已跟踪 63 个文件） | 已在本轮重写中修正；建议把"文档与 git 状态核对"固化为发版检查项 |
| P2-8 | Python 版本口径不一致（3.8 / 3.10 / 3.14 三处表述） | 已在新 README 中统一为"完整功能 ≥3.10" |
| P2-9 | `info-transfer` 的 python-docx/xlrd/olefile 依赖：代码与 `requirements.txt` 中均存在（精简传输模式可选使用），但部分文档称"已移除该依赖" | 已在新文档中按可选依赖表述 |
| P2-10 | 多个 `manifest.features` 描述过时（case-report "规则/大模型"、info-transfer "文档提取"、knowledge-base "只读安全"现已支持下载） | 随功能变更同步 manifest |

---

## 六、行动建议（优先级排序）

### 立即处理（本次发版前）

1. 补回或移除两个 `prompt.json` 模板（P0-1）
2. `build-deploy.ps1` 清理清单加白名单，保住 `trajectory-sketch/backend/config.json` 模板（P0-2）
3. `config/data_root.json` 移出版本库（P0-3）
4. `file-filter` 的 `def status()` → `ff_status`（P0-5）
5. 重新打包并递增 `-Version`，验收最新知识库预览（P0-4）

### 短期（1~2 个迭代）

6. 按 §4.1 修订《插件设计规范》（N-1~N-5），使其与实现一致
7. 统一前端 `?v=` 纪律，修正 admin 各页（P1-2）
8. 清理随包分发的开发/测试脚本与演示数据（P1-3、P1-4）
9. 统一 SVG 图标回退为单一维护点，补 `file-filter` 回退（P1-1）
10. 首页过滤空分类（P1-5）

### 中期

11. 建立 `tests/` 与 CI，把隔离数据根打桩手法固化为 fixture（P2-5）
12. 清理历史分支与构建产物（P2-1、P2-3）
13. 补充第三方组件与许可证清单（P2-4）
14. 按 §4.2 补充规范条款（N-6~N-11）
15. 评估生产部署形态（Gunicorn + Nginx）与 B 型插件规范的去留（P2-6、N-15）

---

## 七、结语

JZToolsHub 的插件化架构是**成功**的：16 个插件形态各异（纯前端、大模型、视频处理、Office 解析、离线传输），却都收敛在同一套目录约定、路由约定、数据约定之下，且移除任何一个都不留残骸——这证明"目录即插件、配置即启停、隔离即安全"三条铁律是落地的，而不是口号。

真正需要投入的不是重构，而是**把实战中沉淀的约定回写进规范**（数据根目录、endpoint 前缀、模板同步登记、程序化接口、图标回退、版本号纪律），并**修好 4 条已经让机制静默失效的问题**（P0-1~P0-4）。做完这两件事，这个项目的可接手性与可维护性会有量级提升。

---

*JZToolsHub 评估报告 · 2026-09-14*
