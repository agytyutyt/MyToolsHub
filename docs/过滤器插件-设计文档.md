# 过滤器插件（file-filter）设计文档

> 状态：**开发中（TODO 断点续开发用）** · 设计版本 v1.0 · 2026-09-10
> 插件 ID：`file-filter`（目录 `plugins/file-filter/`）· 类型：L2 前后端一体（A 型源码插件）
> 依据：《插件设计规范.md》v1.1（B-1~B-8、SEC-1~6 等）· 项目 README · HANDOFF.md

---

## 1. 需求与概述

对用户上传的表格文件进行**字段脱敏（列过滤）**与**合规性检查**：

| # | 需求 | 实现要点 |
| --- | --- | --- |
| 1 | 页面左右两块：左侧输入区（虚线方框上传控件），右侧输出结果区（提供过滤后文件下载） | 前端 flex 布局，上传后即时预览字段 |
| 2 | 上传文件限制 `xlsx` / `xls` / `csv` | 后端 SEC-3 白名单 + 大小上限 20MB |
| 3 | 两种过滤模式：硬过滤 / 大模型过滤 | 单选；大模型模式复用插件级 LLM 配置 |
| 4 | 硬过滤：按固定字段名保留列，其余删除（精确匹配） | `core.filter_columns()` |
| 5 | 大模型过滤：提取全部字段（标题）交大模型判断与保留字段名是否关联（如「时间」↔「开始时间」），匹配保留、其余去除 | `llm_client.match_columns()`，批量一次调用 |
| 6 | 后处理：管理员预设文本规则，检测并去除单元格/表头中的相应文本；支持正则（如内容「开始时间」+ 规则「开始」→「时间」） | `core.post_process()`，规则含 `is_regex` 开关 |
| 7 | 符合插件规范；主要功能（过滤、后处理）提供接口供其他插件调用 | `POST /api/file-filter/apply`（同步 JSON-in/JSON-out，不落用户文件） |

## 2. 业务流程

```
用户上传文件（xlsx/xls/csv，≤20MB）
   │  解析为二维表 rows + headers（csv 用标准库，xlsx 用 openpyxl，xls 用 xlrd）
   ▼
[模式一 硬过滤] headers ∩ keep_columns（精确匹配，trim/去 BOM/全角转半角后比较）
[模式二 大模型过滤] headers → LLM 判定每个表头与 keep_columns 中哪个字段关联
   │  （LLM 未配置/调用失败 → 任务 error，可回退用硬过滤结果提示用户）
   ▼
[后处理 post_process]（管理员配置，两种模式后都执行）
   - 文本替换规则：{pattern, replacement, is_regex, enabled}
   - 作用于：表头 + 数据单元格（字符串值）
   ▼
生成输出文件（保持原格式 xlsx/xls→xlsx；csv→csv 带回车换行与 BOM 兼容）
   │  落盘 .task_cache/<task_id>/output.xxx（TTL 30 分钟）
   ▼
右侧结果区：保留字段列表 / 删除字段列表 / 替换次数统计 / 下载按钮
```

## 3. 目录结构（S-1/S-2）

```
plugins/file-filter/
├── manifest.json            # id=file-filter，icon=🧹
├── README.md
├── frontend/
│   ├── index.html           # 入口（资源引用带 ?v=1，F-2）
│   ├── app.js
│   └── style.css
└── backend/
    ├── __init__.py
    ├── routes.py            # register(app)，/api/file-filter/*
    ├── core.py              # 读写表格 + 过滤 + 后处理（供 routes 与外部插件复用的纯函数层）
    ├── llm_client.py        # OpenAI 兼容 chat 客户端 + 字段语义匹配
    └── requirements.txt     # openpyxl / xlrd / requests
```

## 4. 数据与配置（9.1 数据归属）

- 运行数据：数据根目录 `plugins/file-filter/.task_cache/`（任务产物，TTL 30 分钟清理）。
- 管理员配置：数据根目录 `plugins/file-filter/config.json`：
  ```json
  {
    "llm": {"base_url": "", "api_key": "", "model": ""},
    "keep_columns": ["时间", "地点", "人员"],
    "post_rules": [{"pattern": "开始", "replacement": "", "is_regex": false, "enabled": true}]
  }
  ```
- `config.json` 含 API Key → 已 gitignore（SEC-2）；模板同步无需登记（首次保存自动生成；
  如需随版本推默认值，后续在 `jztools_data._TEMPLATE_SYNC` 与 `install.ps1` 两处补 `ensure-keys` 登记）。
- 过滤任务产物属临时文件，非「用户产生的数据」记录，不含归属四字段（与 trajectory-convert 同口径，
  但沿用其 R6 纵深防御：任务记录 `created_by`，轮询/下载校验任务归属——非创建者且非超管 404）。

## 5. 后端接口设计（B-2 前缀 `/api/file-filter`）

| 接口 | 方法 | 说明 | 权限 |
| --- | --- | --- | --- |
| `/api/file-filter/status` | GET | 依赖自检（openpyxl/xlrd/requests + LLM 是否已配置） | 登录 |
| `/api/file-filter/config` | GET | 读取配置（api_key 掩码回传 + `can_manage`） | 登录 |
| `/api/file-filter/config` | POST | 保存配置——**仅管理员/超管**（keep_columns、post_rules、llm） | 管理员 |
| `/api/file-filter/config/test` | POST | LLM 连通性测试 | 管理员 |
| `/api/file-filter/filter` | POST | multipart：`file` + `mode`(hard/llm) + `columns`(JSON 数组，可选缺省用配置)；**异步**返回 `task_id` | 登录 |
| `/api/file-filter/result/<task_id>` | GET | 轮询 `{status, kept, removed, replace_count, error, download, filename}`；任务归属校验 | 创建者/超管 |
| `/api/file-filter/download/<task_id>` | GET | 下载过滤后文件（SEC-1：ID 白名单正则防穿越） | 创建者/超管 |
| `/api/file-filter/apply` | POST | **程序化调用接口（供其他插件）**：JSON `{rows, mode, columns, post_rules?}` 同步返回 `{rows, kept, removed, replace_count}`；`post_rules` 不传时用管理员配置。不落盘 | 登录（插件间经前端/后端 fetch，鉴权走框架 session） |

### 5.1 核心纯函数（core.py，供外部插件 import 复用）

> B-7 允许其他插件通过框架动态加载机制按 `jztools_file_filter.core` 方式引用吗？
> ——**规范 B-7 禁止 import 其他插件后端模块**。因此「供其他插件调用」统一走 **HTTP 接口 `/apply`**：
> 其他插件后端用 requests 带 session 调用，或其前端直接 fetch。core.py 只服务本插件。

```python
read_table(path, filename) -> (headers, rows)     # csv/xlsx/xls 自适应；单元格转字符串（保留数字）
filter_columns(headers, rows, keep) -> (headers2, rows2, kept, removed)   # 硬过滤：精确匹配（clean 后）
post_process(headers, rows, rules) -> (headers2, rows2, count)            # 文本/正则替换
write_table(path, ext, headers, rows)                     # xlsx(xlsx)/csv；xls 输出转 xlsx
llm_match_columns(headers, keep, base_url, key, model, timeout) -> {header: matched_keep or None}
```

### 5.2 LLM 字段匹配协议

- 一次调用传入全部表头与保留字段名单，要求输出 JSON：
  `{"mappings": {"表头": "对应保留字段或空", ...}}`（temperature=0.1）。
- 复用 case-report 的 `llm_client` 模式：`parse_model_output` 容忍 markdown 围栏、
  `chat_json` OpenAI 兼容 /chat/completions。
- 匹配后校验：返回值必须 ∈ keep 名单（或空），否则视为未匹配（防模型幻觉映射到不存在字段）。

## 6. 前端设计（F-1~F-6）

- 左右两栏（`display:flex`）：**左=输入区**（虚线方框上传控件，拖拽+点击、格式校验 xlsx/xls/csv、
  上传后展示文件名与字段预览 chips；模式单选：硬过滤/大模型过滤；保留字段多选框——管理员配置的
  keep_columns 生成，普通用户只勾选不做文本编辑）+「开始过滤」按钮；**右=输出区**（结果状态、
  保留/删除字段对照、替换统计、下载按钮、错误提示）。
- 管理员（`can_manage=true`）显示「⚙️ 管理配置」面板：保留字段管理（增删）、后处理规则表格
  （pattern/replacement/is_regex/enabled）、LLM 配置（base_url/api_key/model + 测试连通）。
- XSS：所有动态渲染用 `textContent`/转义（F-3）；资源引用带 `?v=1`（F-2）。

## 7. 安全与健壮性（SEC 落实）

| 条款 | 落实 |
| --- | --- |
| SEC-3 上传校验 | 扩展名白名单 xlsx/xls/csv；≤20MB（413）；落盘文件名用 task_id 重命名，不用原始文件名 |
| SEC-1 穿越拦截 | task_id 匹配 `^[A-Za-z0-9]+$` 才拼路径；send_file 限定在 .task_cache 内 |
| SEC-4 匿名 | 全接口走框架登录拦截（无自定义白名单） |
| SEC-5 异常 | 非预期异常返回类型名不含堆栈/路径 |
| SEC-6 外呼超时 | LLM 请求 timeout=120s，解析包裹异常 |
| 长任务（8.5） | ThreadPoolExecutor(2) + task_id 轮询 + TTL 30 分钟 + 任务归属校验 |
| B-8 日志 | 不自行落盘日志；`set_operation("过滤表格")` 等标记 |

## 8. 异常与边界

- 空表/无表头：400 `{"error": "表格为空或缺少表头"}`。
- 硬过滤零匹配：正常执行（结果全删），结果区明确提示「没有匹配任何保留字段」。
- LLM 未配置：任务直接 error，detail 提示去配置面板（前端引导）。
- LLM 返回非法映射：未在保留名单中的映射值按未匹配处理（不中断）。
- CSV 编码：utf-8-sig 读取（兼容 BOM/GBK 场景退回 gbk 重试）；写出 utf-8-sig（Excel 打开不乱码）。
- .xls 输出：统一转 .xlsx（xlrd 只读）；前端下载名提示格式变化。
- 大表防护：行数 ≤ 100000、列数 ≤ 256，超限 400。

## 9. TODO（断点续开发清单）

- [x] T1 设计文档（本文件）
- [x] T2 插件骨架：manifest.json / frontend/ / backend/（__init__.py、requirements.txt）
- [x] T3 core.py：read_table / filter_columns / post_process / write_table / clean_header
- [x] T4 llm_client.py：chat_json / parse_model_output / match_columns
- [x] T5 routes.py：status / config / config/test / filter / result / download / apply
- [x] T6 前端：index.html / app.js / style.css（左右布局 + 虚线上传 + 管理面板）
- [x] T7 注册：config/tools.json（模板 + 数据根目录副本）
- [x] T8 插件 README.md
- [x] T9 测试：py_compile + test client 回归（登录→config→硬过滤→后处理→apply→result/download）
- [x] T10 同步 README.md / HANDOFF.md

## 9.1 修订记录

- v1.1（2026-09-10）修复两处缺陷 + 一处测试事故：
  1. **保存配置无提示**：`saveConfig` 曾把提示写到左侧过滤状态栏（`filterStatus`）；
     现配置卡片内新增 `cfgSaveTip`（✓ 配置已保存 / 失败原因，3 秒自动隐藏）。
  2. **大模型误报未配置**：前端曾读取后端从未返回的 `llm_key_configured` 字段；
     现 `GET /config` 返回 `llm_configured`（基于真实 Key 状态），前端据此判断；
     另后端 `POST /filter` 对 `mode=llm` 增加前置校验（Key 为空直接 400，不进队列，8.5-3）。
  3. **测试事故**：回归测试曾直接写真实数据根目录 `config.json`，覆盖过用户的
     保留字段名单 / 后处理规则 / LLM 地址与模型名（API Key 因「留空不修改」幸存）；
     已改为「备份-恢复」隔离（atexit 原样恢复），并补充掩码回传 / 留空不改 Key 用例。
     前端资源版本号递增至 `?v=2`（F-2）。
- 同步注意：源码插件改动后需整体复制到 `deploy/JZToolsHub/plugins/file-filter/` 并重启
  部署版服务（卡片/前端清单每请求实时读，后端路由随启动注册）。

## 10. 验收自检（规范 14 章节选）

- [x] 目录名 = manifest.id = tools.json id = `file-filter`
- [x] manifest 合法 JSON；entry=index.html 存在
- [x] 前端资源相对路径 + `?v=1`；textContent 防 XSS
- [x] backend/__init__.py 存在；routes.py 导出 register(app)；全部路由挂 /api/file-filter/
- [x] 异步任务：线程池 2 + TTL 30min + 归属校验
- [x] /status 依赖自检；缺依赖优雅降级
- [x] requirements.txt：openpyxl / xlrd / requests
- [x] 数据只写数据根目录 plugins/file-filter/；config.json 已 gitignore
