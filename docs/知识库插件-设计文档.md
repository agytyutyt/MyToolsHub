# 知识库插件（knowledge-base）开发设计文档

| 文档属性 | 内容 |
| --- | --- |
| 文档版本 | v1.0（2026-09-08） |
| 插件 ID | `knowledge-base`（目录名、API 前缀、权限点三同） |
| 插件类型 | L2 前后端一体（A 型源码插件） |
| 状态 | **设计定稿，开发未开始** —— 按 §11 TODO 逐项执行，断点续开发以本文档 + §11 为准 |
| 关联规范 | 《插件设计规范.md》（B-1~B-8、SEC-1~6、F-1~F-7、9.2 归属铁律）、《HANDOFF.md》§7.5 |

---

## 1. 需求与边界

### 1.1 需求

- 管理员（管理员角色/超管）可上传 PDF / OFD / Word / Excel / Markdown 等文档，文件保存在插件数据目录中，可**分级分类**管理（多级分类树 + 文件归档）。
- 普通登录用户可浏览分类、点击阅读。
- 阅读界面 Material Design 风格，与站点一体；**仅阅读 + 复制**，禁止修改/编辑。

### 1.2 明确的边界（不做）

| 不做 | 理由 |
| --- | --- |
| 不使用浏览器内置 PDF 播放器 / Adobe 等浏览器控件 | 需求红线；全部由纯 JS 库在前端渲染 |
| 不提供在线编辑、批注、协作 | 需求明确只读 |
| 不提供下载按钮 | "仅阅读、复制等基础功能"；后端也不提供 attachment 下载端点 |
| 不做全文检索（第一版） | 低性能需求，仅分类过滤 + 文件名搜索 |
| 不支持旧版 `.doc`（二进制格式） | 前端无成熟纯 JS 渲染方案；上传白名单直接拒绝并提示另存为 `.docx` |

---

## 2. 可复用调研结论（GitHub）

全部选型为 **纯前端 JS 渲染库**（无浏览器插件/控件依赖），统一 vendor 到 `frontend/vendor/`（规范 F-7：内网部署库文件进自己插件目录，禁止运行时 CDN 依赖）。

| 格式 | 库 | 仓库 / Star | 许可证 | 使用方式 |
| --- | --- | --- | --- | --- |
| PDF | **pdf.js**（pdfjs-dist **legacy** 构建，兼容老 Chrome） | mozilla/pdf.js ★53.8k | Apache-2.0 | Canvas 逐页渲染 + `getTextContent()` 实现"复制本页文字" |
| OFD | **easyofd**（Canvas 渲染 + 内置文本选择层，页码/缩放/取文本 API 齐备） | easyofd npm 1.2.1（张新攀，执行 GB/T 33190） | Apache-2.0 | `new EasyOFD(id, el)` → `loadFromBlob()`；`GetPageText()` 实现复制；自带工具栏/水印以 CSS 隐藏，统一用本插件 Material 顶栏驱动 |
| Word `.docx` | **mammoth.js**（mammoth.browser.min.js，含依赖单文件） | mwilliamson/mammoth.js ★6.3k | BSD-2-Clause | `convertToHtml({arrayBuffer})` → DOMPurify 清洗后渲染 |
| Excel `.xlsx/.xls` | **SheetJS CE**（xlsx.full.min.js） | SheetJS/sheetjs | Apache-2.0 | `XLSX.read()` → `XLSX.write(_, {type:'string', bookType:'html'})` 按 sheet 渲染表格 |
| Markdown | **marked**（UMD 单文件）+ **DOMPurify** | markedjs/marked ★37.1k；cure53/DOMPurify | MIT；Apache-2.0/MPL-2.0/GPL | `DOMPurify.sanitize(marked.parse(text))`（marked 官方推荐组合） |
| 纯文本 | 无需库 | — | — | `textContent` 渲染（天然无 XSS） |

> 版本锁定与获取方式见 §11 阶段 1；**vendor 文件入库**（它们是插件源码的一部分，不含密钥，符合 SEC-2）。
> mammoth 安全提示：不清洗源文档 → 必须过 DOMPurify 后再 innerHTML；pdf.js/ofd/mammoth 的输出同样先消毒。

---

## 3. 架构设计

```
外壳页 /tool/knowledge-base（iframe 隔离）
└── plugins/knowledge-base/frontend/index.html   ← 单页双视图：库视图 / 阅读视图
    ├── 库视图：左侧分类树（分级）+ 右侧文件卡片列表 + 搜索 + 上传（仅管理员）
    └── 阅读视图：Material 顶栏（返回/标题/翻页/缩放/复制）+ 只读渲染区

plugins/knowledge-base/backend/routes.py        ← /api/knowledge-base/*
    ├── 分类 CRUD（仅管理员写）
    ├── 文件上传/删除（仅管理员写，SEC-3 校验）
    ├── 文件列表（所有登录用户）
    └── 文件内容流 /files/<id>/raw（Flask send_file，支持 Range 断点，
        attachment 头不设置 → 仅内联供渲染）

数据根目录 plugins/knowledge-base/
├── data/categories.json    # 分类树（单库文件，带 RLock 写）
├── data/files.json         # 文件元数据索引（单库文件）
└── data/files/<file_id>.<ext>   # 上传的原始文件（服务端生成 ID 命名，SEC-3）
```

### 3.1 前后端职责

- **后端只做"存与管"**：鉴权、校验、落盘、元数据；不理解文档内容（解析渲染全在前端）→ 后端零重依赖（无 python-docx/pdf 库），`requirements.txt` 为空。
- **前端只做"读"**：按扩展名分派渲染器；所有库文件惰性按需 `<script>` 注入（打开 PDF 才加载 pdf.js），降低初始体积 → 低性能需求。

### 3.2 权限模型

| 动作 | 谁 | 实现点 |
| --- | --- | --- |
| 上传文件 / 新建·改名·删除分类 / 移动·删除文件 | 管理员角色（`role-admin` / 角色名「管理员」）或超管 | 仿 notice-board `_can_publish()` 判定，后端强校验 |
| 浏览分类、阅读文件、复制 | 全体登录用户 | tools.json 登记 `grant_all: true`；前端入口仅按角色隐藏管理按钮 |
| 未登录 | 一律被框架 302/401 拦截 | SEC-4，插件不另开白名单 |

### 3.3 数据归属（规范 9.2 铁律）

- 分类、文件元数据落盘记录 `created_by` / `created_by_name` / `unit_id` / `department_id` 四字段，**一律取自服务端 session，禁止接收请求体传入**。
- 阅读属"全站公共资源"：不做按单位/部门过滤（列表对所有登录用户可见）；归属四字段仅用于审计与排序展示（"上传人"）。

---

## 4. 目录结构（规划）

```
plugins/knowledge-base/
├── manifest.json            # id=knowledge-base, icon=📚, accent=#5C6BC0(靛蓝), features=[在线阅读, 分类管理]
├── README.md                # 按 S-5 附简短说明
├── frontend/
│   ├── index.html           # 入口（?v=1 起步，F-2 纪律）
│   ├── style.css            # Material 风格（对齐 shared-docs 的 CSS 变量体系）
│   ├── app.js               # 库视图 + 视图切换 + API 封装
│   ├── reader.js            # 阅读视图 + 各格式渲染器分派
│   └── vendor/              # ★ 离线内嵌第三方库（见 §11 阶段1 清单）
│       ├── pdf/pdf.min.js / pdf.worker.min.js      # pdfjs-dist legacy
│       ├── ofd/easyofd.js + x2js/jszip/opentype/eaysjbig2  # easyofd 及其依赖
│       ├── mammoth/mammoth.browser.min.js
│       ├── xlsx/xlsx.full.min.js
│       ├── marked/marked.min.js
│       └── purify/purify.min.js
└── backend/
    ├── __init__.py          # 空文件
    ├── routes.py            # register(app)，前缀 /api/knowledge-base
    └── requirements.txt     # 空或仅注释（无第三方依赖）
```

---

## 5. 数据模型

### 5.1 categories.json（单库文件 + 全局 RLock）

```jsonc
{
  "version": 1,
  "categories": [
    {
      "id": "cat-xxxxxxxx",        // 服务端生成 "cat-" + 8位hex，正则 ^[A-Za-z0-9_-]+$
      "name": "培训资料",           // ≤40 字符，同层不重名（后端校验）
      "parent_id": null,           // null=根；支持多级（前端限制展示缩进深度≤4，超深层照样可用）
      "order": 1,
      "created_by": "admin",
      "created_by_name": "管理员",
      "unit_id": null,
      "department_id": null,
      "created_at": "2026-09-08T10:00:00"
    }
  ]
}
```

### 5.2 files.json（单库文件）

```jsonc
{
  "version": 1,
  "files": [
    {
      "id": "f-xxxxxxxx",                // 服务端生成；同时是落盘文件名主键
      "name": "某某培训课件",             // 展示名（可改名），≤80 字符
      "original_name": "培训课件.pdf",    // 原始文件名（仅展示，禁止用于拼路径，SEC-3）
      "ext": "pdf",                      // 归一化小写扩展名 = 类型分派依据
      "size": 1234567,                   // 字节
      "category_id": "cat-xxxxxxxx",
      "created_by": "admin",             // ↓ 归属四字段（铁律一）
      "created_by_name": "管理员",
      "unit_id": null,
      "department_id": null,
      "created_at": "2026-09-08T10:00:00",
      "updated_at": null                 // 改名/移动时刷新
    }
  ]
}
```

### 5.3 落盘文件

`data/files/<id>.<ext>`——`<id>` 必须匹配 `^[A-Za-z0-9_-]+$`（SEC-1 目录穿越防线同款正则），扩展名白名单二次确认后才落盘。

---

## 6. API 设计（前缀 `/api/knowledge-base`，全部 B-2 合规）

| 方法 | 路径 | 权限 | 说明 |
| --- | --- | --- | --- |
| GET | `/status` | 登录 | 依赖自检（本插件后端无第三方依赖，恒 ok；报告 vendor 库清单供前端诊断） |
| GET | `/config` | 登录 | `{can_manage}`（管理员判定，前端据此显示/隐藏管理 UI） |
| GET | `/categories` | 登录 | 分类树（数组，前端组树） |
| POST | `/categories` | 管理员 | `{name, parent_id?}`；同层重名 409；父分类不存在 404 |
| PUT | `/categories/<id>` | 管理员 | `{name?, parent_id?, order?}`；parent 不得指向自身/自身子孙（防环） |
| DELETE | `/categories/<id>` | 管理员 | 分类下有子分类或文件时 409 拒绝（要求先清空） |
| GET | `/files?category=&q=` | 登录 | 文件列表：category 为分类 id（或 `root`）；q 模糊匹配展示名/原始名；均不传=全量。返回含上传人姓名、大小、时间 |
| POST | `/files` | 管理员 | multipart：`file` + `name` + `category_id`。校验：扩展名白名单、大小 ≤ 20MB（SEC-3）；落盘用服务端 ID 重命名 |
| PUT | `/files/<id>` | 管理员 | `{name?, category_id?}` 改名 / 移动分类 |
| DELETE | `/files/<id>` | 管理员 | 删元数据 + 删落盘文件（不存在 404） |
| GET | `/files/<id>/raw` | 登录 | **内联**返回文件内容（`send_file`，`conditional=True` 支持 Range；不设 attachment 头 → 不可"另存为下载"）。不可读（不存在/ID 非法）一律 404 |

错误风格统一 `{"error": "..."}` + 4xx（8.4）；越权写 403、资源不存在一律 404（不暴露存在性）。

**支持上传的白名单**：`pdf, ofd, docx, xlsx, xls, md, markdown, txt, csv`。超过 20MB / 白名单外 → 415/413 明确提示（`.doc` 提示"请另存为 .docx 后上传"）。

---

## 7. 前端设计（Material Design）

### 7.1 视觉体系

- 复用站点 Material 风格语言（参考 shared-docs 的 CSS 变量模式）：`:root` 定义 `--accent/--bg/--card/--text/--border/--shadow/--radius`；accent 取 `#5C6BC0`（Indigo 400，与既有插件不撞色）。
- Material 组件自绘（零框架，与站点一致）：顶栏（elevation 阴影 + 返回箭头）、卡片（圆角 12px + 投影 + hover 抬升）、FAB（管理员上传，右下角）、 chips（类型徽标：PDF 红/Word 蓝/Excel 绿/OFD 青/MD 橙/TXT 灰）、Snackbar（操作反馈）、对话框（上传/删除确认/新建分类）。
- 全屏按钮沿用站点 `.btn-fullscreen` 模式。

### 7.2 库视图（index.html + app.js）

```
┌──────────────────────────────────────────────────────┐
│ 知识库  [搜索框]                          (全屏) (＋上传)│
├──────────────┬───────────────────────────────────────┤
│ ▾ 全部        │ [卡片] 名称        [PDF] 1.2MB · 张三    │
│   ▾ 培训资料   │        2026-09-01          (阅读)      │
│   ▸ 规章制度   │ [卡片] ……                             │
│              │                                       │
│ (管理员: +分类 │  空态：分类下暂无文件                   │
│  重命名/删除) │                                       │
└──────────────┴───────────────────────────────────────┘
```

- 分类树可折叠（details/summary 或自绘），选中态高亮；文件列表按 `created_at` 倒序。
- 管理员专属操作按 `config.can_manage` 显隐（前端隐藏仅是体验，后端强校验兜底）。
- 上传对话框：选择文件 + 展示名 + 目标分类（默认当前选中分类），逐个上传显示进度（fetch 不支持上传进度 → 以"上传中…"状态即可，低性能需求）。

### 7.3 阅读视图（reader.js）

```
┌──────────────────────────────────────────────────────┐
│ ←  课件名称.pdf        [第 2/38 页][－ 100% ＋] [复制] │
├──────────────────────────────────────────────────────┤
│                 （只读渲染区，居中纸张样式）             │
└──────────────────────────────────────────────────────┘
```

**渲染器分派矩阵**（按元数据 `ext`）：

| ext | 渲染方式 | 复制实现 |
| --- | --- | --- |
| `pdf` | pdf.js：Canvas 逐页渲染，页码导航 + 缩放（0.6~3.0 步进） | 「复制本页」按钮：`page.getTextContent()` 拼接文本 → 剪贴板 |
| `ofd` | ofd.js：`parseOfdDocument` 后逐页 `renderOfdByIndex` 注入 SVG；页码导航（宽度取容器宽） | OFD 文字层不可靠 → 「复制本页」从解析出的文本节点提取（尽力而为，提取不到提示不支持复制） |
| `docx` | mammoth → HTML → **DOMPurify 清洗** → 纸张样式渲染；表格/图片内联 | 原生选区 + 「复制全文」（取 `innerText`） |
| `xlsx/xls/csv` | SheetJS → 每 sheet `bookType:'html'` 表格；sheet 标签页切换；横向滚动 | 原生选区（表格文本天然可复制） |
| `md/markdown` | marked → **DOMPurify 清洗** → 文档样式渲染 | 原生选区 + 「复制原文」（MD 源文本） |
| `txt` | `textContent` 渲染（等宽保留换行） | 原生选区 |

### 7.4 通用约束

- **只读保障**：渲染容器不用 `contenteditable`；无任何写接口调用；`user-select` 保持可用（复制是需求）；右键菜单不禁用。
- **惰性加载**：库视图仅加载 app.js；进入阅读视图按 ext 动态注入对应 vendor `<script>`（带加载失败降级提示"渲染库缺失"）。
- **资源版本**：所有 vendor 引用从第一天起带 `?v=1`（HANDOFF §6.6 教训）。
- **会话超时**：fetch 包装器对 401/302 统一跳转登录（对齐框架行为）。
- PDF 大文件：逐页渲染 + `render_task.cancel()` 防连点堆积；worker 用同目录 `pdf.worker.min.js`。

---

## 8. 安全清单对照

| 规范 | 落实 |
| --- | --- |
| SEC-1 目录穿越 | 文件 ID 白名单正则 `^[A-Za-z0-9_-]+$`；落盘路径仅 `DATA_DIR/files/<id>.<ext>`（ext 二次白名单）；raw 接口拒绝一切路径参数 |
| SEC-3 上传 | 扩展名白名单 + 20MB 上限 + 服务端生成 ID 重命名；原始文件名只进元数据字段 |
| SEC-4 匿名 | 框架全局拦截，插件不设白名单；所有接口第一步取 `get_session_user()`，取不到 401 |
| SEC-5 泄漏 | 404/403 不区分"不存在"与"无权"；异常不回堆栈 |
| XSS | mammoth/marked 输出必过 DOMPurify；txt 用 textContent；文件名等插值一律转义 |
| 9.2 铁律 | 归属四字段 session 取值；列表接口对所有登录用户开放为本插件明确设计（§3.3） |
| B-8 日志 | 不自落日志；`set_operation("上传知识库文件" / "删除分类" 等)` 标记写操作 |

---

## 9. 性能策略（低性能需求）

1. **无后端解析**：后端零第三方依赖，不做文档解析；CPU 消耗全部在客户端、且仅阅读时发生。
2. **惰性 vendor**：按格式注入脚本，库视图初始 JS < 100KB。
3. **单库 JSON + RLock**：分类/文件元数据各一个 JSON 文件，读写加全局锁（量级：百级文件，无性能问题）；文件字节流由 `send_file` 直出（etag/Range/304 由框架处理）。
4. **PDF 逐页渲染**：同一时刻仅渲染当前页，翻页取消上一个渲染任务。
5. gzip：`.md/.txt` 与 API JSON 走框架统一 gzip（app.py 中间件），无需插件处理。

---

## 10. 风险与预案

| 风险 | 预案 |
| --- | --- |
| ofd.js 维护度低、复杂 OFD 渲染不全 | 接受"尽力渲染"；渲染失败降级为提示"该 OFD 文件暂无法在线渲染"；库文件锁定经测试的版本不再追新 |
| 老旧 Chrome（内网 Chrome 72/78）兼容 | pdf.js 用 legacy 构建；其余库均为 UMD/ES5 产物；避免 `?.`/`??`/`gap`（站点已有 Chrome<84 fallback 惯例） |
| vendor 下载不可达（离线开发机） | 阶段 1 阻塞时在 §11 记录缺失清单，先开发前后端，最后补库（代码已按惰性加载设计，库缺失只影响对应格式） |
| `.doc`/`.xls` 老格式 | `.xls` SheetJS 支持；`.doc` 白名单拒绝并提示转存 `.docx` |
| 大 PDF 内存 | 20MB 上限 + 逐页渲染已兜底；渲染异常 catch 后 Snackbar 提示 |

---

## 11. TODO（断点续开发清单 · 按序执行）

> 约定：完成一项把 `[ ]` 改 `[x]` 并附日期；发现新事项追加到对应阶段末尾，不要打乱顺序。
> 阶段 2/3/4 可并行；阶段 1 阻塞不影响 2~4 开发（惰性加载兜底）。

### 阶段 0：准备 ✅（本文档）

- [x] 阅读 README / HANDOFF / 插件设计规范
- [x] GitHub 调研渲染库并定稿选型（§2）
- [x] 设计文档定稿

### 阶段 1：vendor 第三方库（阻塞项：外网可达性）✅ 2026-09-08

- [x] 建目录 `plugins/knowledge-base/frontend/vendor/{pdf,ofd,mammoth,xlsx,marked,purify}/`
- [x] pdf.js legacy：从 npm `pdfjs-dist@3.11.174`（legacy 产物，兼容老 Chrome）取 `legacy/build/pdf.min.js` + `legacy/build/pdf.worker.min.js`（jsdelivr）
- [x] OFD：**改选 easyofd@1.2.1**（DLTech21 ofd.js 1.5.0 依赖外部 FreetypeKit.wasm 字体引擎且无 license 强制加"demotest"水印，无法离线自包含 → 弃用；easyofd 纯 JS + Canvas + 内置文本选择层，依赖 x2js/jszip/opentype/eaysjbig2 全部 vendor）
- [x] mammoth：`mammoth.browser.min.js`（npm mammoth@1.6.0，jsdelivr）
- [x] SheetJS：`xlsx.full.min.js`（npm xlsx@0.18.5 CE，jsdelivr）
- [x] marked：`marked.min.js`（npm marked@4.3.0 UMD，jsdelivr）
- [x] DOMPurify：`purify.min.js`（npm dompurify@3.0.6，jsdelivr）
- [x] 完整性核对 + `vendor/VENDOR.md` 登记来源/版本/许可证/加载顺序
- [ ] 浏览器冒烟（并入阶段 4 阅读器联调：`pdfjsLib/EasyOFD/mammoth/XLSX/marked/DOMPurify`）

### 阶段 2：后端

### 阶段 2：后端 ✅ 2026-09-08

- [x] 建目录骨架：`backend/{__init__.py,routes.py,requirements.txt}`、数据目录经 `jztools_data.get_data_root_dir("plugins","knowledge-base",...)` 定位（对齐 notice-board 写法）
- [x] `register(app)` + 会话/日志样板（`get_session_user` / `set_operation` try-except 兜底导入）
- [x] 存储层：`_load_store/_save_store`（原子写 tmp+os.replace + RLock）、categories/files 结构体
- [x] 分类 CRUD + 防环 + 同层重名 409 + 非空删除保护 409（管理员校验 403）
- [x] 文件列表（category 递归含子孙分类 / q 模糊过滤、倒序、含上传人）
- [x] 上传：multipart 校验（白名单/20MB 413/空文件/服务端 ID 重命名/归属四字段）→ 落盘 `data/files/` → 写 files.json
- [x] 改名/移动（404 校验）、删除（元数据 + 落盘文件同步）
- [x] `GET /files/<id>/raw`：send_file(conditional=True)、Content-Disposition inline（无 attachment）、ID 正则防穿越
- [x] `/status`、`/config`（can_manage 判定仿 notice-board：super_admin 或 role-admin/「管理员」）
- [x] 测试客户端自测 12 项全部 PASS（隔离临时数据根；含 401/403/404/409/413/415/防环/递归列表/inline/raw 穿越防护/落盘对称性）
- [x] `python -m py_compile` 通过

### 阶段 3：前端库视图 ✅ 2026-09-08

- [x] manifest.json / frontend/index.html / style.css / app.js 骨架（`?v=1`）
- [x] CSS：变量体系（accent #5C6BC0）、卡片、树、对话框、Snackbar、FAB、响应式（<860px 树面板转全宽）
- [x] API 封装 + 401 跳登录（`/login?next=`）+ 错误 Snackbar
  - 修复（2026-09-08）：`fetch` init 中 `headers: null` 触发 TypeError（HeadersInit 不可空，
    老 Chrome 直接报 "Failed to read the 'headers' property"）→ 改为有值才放入 init，`app.js?v=2`
- [x] 分类树渲染（折叠箭头/选中态/递归计数，折叠状态存 sessionStorage）+ 管理操作（新建/重命名/删除对话框）
- [x] 文件列表（类型徽标/大小/上传人/时间）+ 搜索（300ms 防抖）+ 空态
- [x] 上传对话框（FAB + 顶栏按钮共用；文件选择自动带出名称）+ 管理员显隐逻辑（config.can_manage）
- [x] 点击文件 → 切阅读视图（返回恢复列表滚动位置）；文件卡片右键 = 管理（改名/移动/删除）

### 阶段 4：前端阅读视图 ✅ 2026-09-08

- [x] reader.js：顶栏（返回/标题/类型徽标/页码导航/缩放/复制/全屏）+ 纸张容器 + 加载/错误态
- [x] 惰性脚本注入器（loadScript Promise 缓存，按 ext 分派，失败降级提示）
- [x] TXT 渲染器（textContent，复制全文）
- [x] MD 渲染器（marked 4.3.0 + DOMPurify，复制原文）
- [x] DOCX 渲染器（mammoth 1.6.0 + DOMPurify，复制全文）
- [x] XLSX/XLS/CSV 渲染器（sheet 标签切换 + 冻结首行 + 复制本表 TSV）
- [x] PDF 渲染器（worker 同目录 / 逐页渲染 + 渲染取消 / 缩放 0.4~3 / 「复制本页」getTextContent 按行拼接 / doc.destroy 清理）
- [x] OFD 渲染器（easyofd 1.2.1：依赖按序加载 x2js→jszip→opentype→eaysjbig2→easyofd；自带工具栏/水印 CSS 隐藏、由本插件顶栏驱动其 FirstPage/NextPage/ZoomIn API；「复制本页」GetPageText；轮询 view.AllPageNo 就绪）
- [x] 复制通道：Clipboard API 优先、execCommand 回退；`node --check` 全部通过
- [ ] 各格式冒烟：手工造样例文件（含中文、表格、图片的 docx/xlsx；多页 PDF；一个真实 OFD）——**待真机浏览器走查（阶段 5 一并进行）**

### 阶段 5：注册与收尾（进行中）

- [x] `config/tools.json`：模板与数据根目录副本均已登记（office 分类，`grant_all: true`, order 6）
- [x] `plugins/knowledge-base/README.md`（功能/技术/限制：不支持 .doc、OFD 尽力而为）
- [x] 同步主 README.md：目录结构、「内置插件一览」、HTTP API 表（knowledge-base 全端点）
- [x] 同步 HANDOFF.md §3（插件一览）与 §7.5（后端速查表）
- [x] 后端自测 12 项 + 集成冒烟 5 项全部 PASS（隔离数据根目录，经官方加载器）
- [x] .gitignore 核对：vendor 入库、`plugins/*/backend/data/` 自动忽略
- [ ] 规范自检清单（插件设计规范 §14 前四组逐项过一遍）
- [ ] 重启服务真机走查：首页卡片 → 上传 → 阅读 6 种格式 → 复制 → 无权限账号无管理按钮 → 退出登录后访问被拦
- [ ] UI 迭代记录（2026-09-08 用户反馈多轮）：去纸张卡片 → 内容居中 → PDF 默认 1.25 →
  顶栏 fixed → 顶栏融合卡片 → **顶栏取消、改底部胶囊 dock**（返回/徽标+标题/翻页缩放/复制/全屏，
  宽度自适应内容）→ 返回最左、全屏最右 → 图标方案定为**内联单色 SVG**（黑白描边 currentColor，
  16px stroke2；不用 emoji/JZIcon，Win7 无字体依赖）；jz-icon.js MAP 曾加 21a9/26f6 后不再被插件引用；
  app.js v6（复制范围改 title 提示）、style.css v11
- [ ] git 提交（用户确认后）

### 遗留/增强（本期不做，可后补）

- [ ] 全文检索（索引 md/txt/docx 文本到内存）
- [ ] 分类可见范围（按单位/部门隔离文件）——如需启用，补做 9.3 过滤即可，数据模型已含归属字段
- [ ] 阅读计数统计
- [ ] PDF 目录/缩略图侧栏
