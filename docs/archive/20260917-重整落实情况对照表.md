# 文档重整落实情况对照表（2026-09-17）

> **归档记录**：本表把《`docs/archive/项目结构梳理与文档整理建议-20260917.md`》的每项建议
> 对应到**具体改动点 / 实施步骤 / 可验证的完成标准**，并如实标注未落实项与原因。
> 执行时间：2026-09-17。**表中路径为重整后的新布局**（旧布局见审计报告自身）。

## 0. 一句话结论

报告提出 **P0 六项 / P1 七项 / P2 六项 + 13 项文档问题（D1–D13）+ 6 项存放位置问题（L1–L6）+ 6 项结构缺陷（S1–S6）**。
本次**落实 P0 六项全部、P1 六项（1 项采用备选方案）、P2 三项**；未落实 5 项，原因与建议见 §4。

---

## 1. P0 —— 六项全部落实

| 编号 | 报告建议 | 具体改动点 | 实施步骤 | 完成标准 / 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- |
| P0-1 | 新增 `docs/README.md` 唯一索引 | 新建 `docs/README.md`（8 节） | ① 定五层结构；② 写「我要做什么 → 看哪份」路由表；③ 写覆盖矩阵；④ 写文档模板与生命周期规范 | 文件存在；§3 覆盖矩阵覆盖全部权威事实；§7 清单与实际文件一一对应 | ✅ |
| P0-2 | 立生命周期标记约定并给文档加状态 | 9 份归档文档加横幅（`docs/archive/*.md`）；16 份现行文档加「四段覆盖 + 覆盖范围/不覆盖/随包/最后核对」块 | ① 归档文档写「原因 + 现行替代」；② 现行文档统一头部块；③ 写进 `docs/README.md` §4/§5 作为规范 | 每份归档文档首屏可见横幅；每份现行文档含「最后核对」字段；`grep -L "最后核对" docs/*/*.md` 仅归档项 | ✅ |
| P0-3 | 修 `HANDOFF.md` §2.1/§2.2 失效状态 | `HANDOFF.md` §2.1 → 3 条自检命令 + 稳定项表；§2.2 → 口径表 + 现取命令；另压缩头部 10,914 字符「第N轮」流水账 | ① 删固化 HEAD/分支/工作区字段；② 版本号类事实改为现取；③ 历史叙述指向 §3 与日志 | §2.1 不含任何提交号；`grep -c "第六轮" HANDOFF.md` = 0；行数 764 → 676 | ✅ |
| P0-4 | 消除同名文件冲突 | `android-app/InfoParse/` 下的桌面端同名手册 → **`InfoParse-APP打包手册.md`** | ① `git mv` 改名；② 批量改写 4 处引用；③ 索引登记 | `find . -name "打包部署手册.md"` 只命中 `docs/guide/` 一份 | ✅ |
| P0-5 | 止住插件测试脚本随包 | `tools/plugin-payload-rules.json`：`exclude_globs` += `test_*.py` / `*_test.py` / `conftest.py`；`exclude_files` += `_build_phase8.py`；`exclude_dirs` += 保留名 `dev`（`tools/dev/` 的验证服务会伪造 admin 会话，绝不能上目标机）；`tools/verify-package.py` 新增 `FORBIDDEN_IN_PACKAGE` 断言 | ① 改单一真源 JSON（两条打包链路共用）；② 加出包后断言；③ 写进口径注释 | `build-deploy-local.py --check-only`：跳过目录 31 → **32**（含 `tools\dev`）、「清理」列表出现 5 个插件测试脚本 + `_build_phase8.py`；`verify-package.py` 断言项存在 | ✅ |
| P0-6 | README §4.1 目录树补漏 | `README.md` §4.1 重写（docs 五层 + 4 个 `test_*.py` + `openh264` DLL + `demo/` + `.bench_corpus/`）；§2.4 加 libopenh264 行；顶部加「文档地图」 | ① 逐项补登记；② 补测试运行方式说明；③ 与 §8/§9 交叉引用对齐 | 报告点出的 7 个未登记项全部出现在 §4.1 | ✅ |

## 2. P1 —— 七项：六项落实，1 项采用备选方案

| 编号 | 报告建议 | 具体改动点 | 完成标准 | 状态 |
| --- | --- | --- | --- | --- |
| P1-1 | 建 `docs/archive/` 并迁入已完结/已取代文档 | 迁入 **10 份**（P0修复方案、09-14 评估报告、v1.0 容器化、多码同屏、2 份 todolist、2 份压缩评估、审计报告本身）；全部加状态横幅 | `docs/` 根下只剩分层目录 + 索引；每份归档件有横幅 | ✅ |
| P1-2 | `docs/` 分层 + 两处随包口径同步 | 建 `guide/design/eval/plan/archive`；`tools/build-deploy-local.py` 的 `COPY_DIRS`/`COPY_FILES`、`build-deploy.ps1` §3.2 同步；`verify-package.py` 加断言 | `--check-only` 计划只新增 `docs/README.md` + `guide/` + `design/`，**不含** `eval|plan|archive` | ✅ |
| P1-3 | 信息传输簇收敛（D1） | 总纲（`eval/信息传输格式开销评估与优化方案.md`）加「覆盖范围/不覆盖」声明；`plan/信息传输优化方案0917.md` 定位为方案专篇；多码同屏、视频流 todolist 归档 | 五份文档职责在 `docs/README.md` §3 覆盖矩阵里互不重叠 | ✅ |
| P1-4 | 两份压缩算法评估合并 | **采用报告 §D2 的备选分支**：两份归档 + 总纲补指针（未合并正文） | 总纲 §5 与 TODO 清单均已指向归档路径；索引标注「结论已合入」 | ⚠️ 备选方案 |
| P1-5 | `移动端APP.md` 代码块契约化 | **采用报告备选方案 B**：加「实现真源」声明 + 四端齐改收尾要求；未剥离 623 行代码 | 文档首屏声明以 `android-app/` 源码为准；`HANDOFF.md` §10.1 已列四端齐改 | ⚠️ 备选方案 |
| P1-6 | 设计文档 TODO 收敛 | `过滤器` / `轨迹速写` 状态改为「已上线」；`知识库插件` 标注 TODO 已基本勾完；2 份已完成 todolist 归档 | 三份文档不再自称「开发中」 | ✅ |
| P1-7 | 脚本迁出 `.workbuddy/`、DLL 补许可证 | 2 个脚本 → `tools/dev/`（修正 `REPO` 相对层级 + 产物改落 `.workbuddy/tmp/`）；`openh264` 许可证登记到 `docs/guide/离线部署包说明.md` §9 + `README.md` §2.4 | 两脚本 `seed-only` 均返回 200；许可证表含 openh264 行 | ✅ |

## 3. P2 —— 三项落实

| 编号 | 建议 | 改动点 | 状态 |
| --- | --- | --- | --- |
| P2-2 | `.workbuddy/` 白名单 gitignore（S4/L2） | `.gitignore` 改为 `.workbuddy/*` + `!.workbuddy/memory/` | ✅ |
| P2-3 | 文档纪律写入规范 | 写入 `docs/README.md` §5（命名/三态/归档触发）与 §6（体量纪律五条） | ✅ |
| P2-4 | 定义随包口径 | `docs/README.md` §8 随包口径表 + `verify-package.py` 出包断言 | ✅ |

## 4. 未落实项（如实记录）

| 编号 | 建议 | 未落实原因 | 建议 |
| --- | --- | --- | --- |
| P2-1 | 新增 `tools/run-all-checks.ps1` 一键校验入口 | 需串联 4 套测试机制（根单测 / 插件单测 / PS 沙箱 e2e / verify-package），且沙箱套件依赖造包；单独立项更稳 | 下个迭代做，入口先落在 `docs/项目管理手册.md` §7 的命令清单 |
| P2-5 | 建 `tests/` 统一测试入口 | 移动根目录 3 个测试文件会破坏 `HANDOFF.md` / 各文档里"在仓库根跑 `python -m unittest <模块>`"的既有约定（多处引用） | 先按 §3 第一步只做**登记**（已完成，见 `README.md` §4.1）；迁移需一次性改全部引用 |
| P2-6 | 产物目录外移 / 历史 zip 清理 | 报告 §6 已判定「不宜草率」：与"两段式 + 增量合并"打包流程耦合，改动需与打包链路一起评估 | 先做低风险的：定期删 `deploy/` 下已被取代的历史 zip（保留当前上线版 + 最近 2 个） |
| — | `移动端APP.md` 完整代码剥离（P1-5 方案 A） | 623 行代码是**刻意的离线参考**（APP 开发者可能无仓库访问），剥离会损失该价值且改动面大 | 单独立项：先统计各代码块与源码的漂移量，再决定逐块替换为指针 |
| — | 两份压缩算法评估**正文合并**（P1-4） | 合并需重写 1000+ 行并核对全部实测数字，收益低于风险（归档已保留证据 + 总纲已吸收结论） | 仅在"有人要复用 DS/Qwen 的许可证与调研矩阵"时再做 |

## 5. 顺带修掉的「文档与代码不一致」7 处

> 这 7 处是执行 P0/P1 过程中**新发现**的（报告未列出），均按代码事实修正：

| # | 位置 | 原表述 | 修正为 |
| --- | --- | --- | --- |
| 1 | `README.md` §10.2 | 「第 2 条门控使新增带模板的插件**不再需要两处手工登记**」 | 更正：自动发现只保证**下次启动**补键；`install.ps1` 走硬编码 `$cfgPairs`，**仍要两处登记**（与同节后文自相矛盾） |
| 2 | `docs/design/登录改造与数据隔离-开发设计文档.md` | 运行环境「Python 3.8」 | 「最低 3.12 / 打包 3.14」（2026-09-14 已取消 3.8 与 Win7 支线） |
| 3 | `plugins/info-transfer/README.md` | 「ppt / pdf / pptx 不支持精简提取，仅原件传输」 | 按 `SUPPORTED_FORMATS` 更正：`ppt`/`pdf` **有**精简提取（→ 纯文本，fmt=text），仅 `pptx` 是 `extract=none` |
| 4 | `docs/design/知识库Office预览-设计文档.md` | 标题与 §3 主张「Word/Excel **转 PDF** 预览」 | 加横幅：该路线 2026-09-13 已被 xhr/dhr 双引擎 **HTML 按需渲染**取代；改名去「插件库优化方案」 |
| 5 | `docs/plan/信息传输优化方案0917.md` | 基线写死「routes.py 2677 行 / manifest 1.1.0」 | 标注为「成文时」，现状以 `manifest.json` 与源码为准 |
| 6 | `docs/design/过滤器插件-设计文档.md`、`轨迹速写插件-设计文档.md` | 状态「开发中（TODO 断点续开发用）」 | 「已上线（插件 v1.1.0）」（TODO 已全部完成） |
| 7 | `HANDOFF.md` §2.2 | 「最新产物：v2.0.0（2026-09-15）」 | 改为只留口径 + 现取命令（版本号类事实一律现取现用） |

## 6. 改动规模（可复核）

| 项 | 数量 |
| --- | --- |
| 迁入 `docs/archive/` | 10 份文档 |
| `docs/` 分层后现行文档 | guide 3 + design 7 + eval 4 + plan 2 = 16 份 |
| 新增文档 | 2 份（`docs/README.md`、`docs/项目管理手册.md`） |
| 批量改写路径引用的文件 | 41 个（`.py` / `.ps1` / `.json` / `.md`） |
| 打补丁的脚本 | 5 个（`build-deploy-local.py` / `build-deploy.ps1` / `verify-package.py` / `plugin-payload-rules.json` / `.gitignore`） |
| 迁移的源码文件 | 2 个（→ `tools/dev/`） |
| 回归 | 29 项 Python 单测全绿；两个 dev 脚本 `seed-only` 返回 200；无断裂 Markdown 表格 |
