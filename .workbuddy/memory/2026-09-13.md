# 2026-09-13 工作日志

## 知识库插件：Excel 预览弃 PDF 改 Python 手绘 HTML 表格

**需求**：用户反馈 xlsx 转成 PDF 预览后**按打印分页**，宽表/长表被切成多页不利阅读。
优化方向：取消 Excel 的 PDF 预览，改为 **Python 手绘**（参考共享文档插件的表格呈现），
不需要支持编辑，尽可能还原表格原样式。要求：先做 TODO 断点续作；先上 GitHub 找方案参考代码。

### GitHub 调研结论
- 选定参考 **Apkawa/xlsx2html**（openpyxl→HTML 样式映射，最匹配）。拉取 core.py /
  format/number.py / constants/builtin.py 等源码研读后**自实现而非引包**，原因：
  1) 目标机内网离线，不能新增 pip 依赖（它还传递依赖 babel）；
  2) 有四处失真要修——theme 主题色直接丢弃、列宽换算 `width/10*96` 偏窄
  （改 Excel 标准 `px≈round(width*7)+5`）、字号把 11pt 当 11px（改 1pt=4/3px）、
  数字格式依赖 babel（自实现常用子集，ROUND_HALF_UP 对齐 Excel 舍入）。

### 交付物
- **新模块 `plugins/knowledge-base/backend/xlsx_render.py`**：openpyxl 读簿（data_only=True
  取公式缓存值）→ 逐 sheet 手绘 `<table>`（`table-layout:fixed` + `<colgroup>` 锁列宽）→
  JSON（各 sheet 的 HTML 片段）原子落盘 `<id>.json`。
  还原：合并单元格（colspan/rowspan，外边框按合并区边缘单元格取值）、列宽/行高/隐藏行列、
  13 种边框样式、填充色（solid/图案近似；**theme+tint** 解析 wb.loaded_theme）、
  字体（名称/字号/加粗/斜体/下划线/删除线/颜色）、对齐（水平/垂直默认 bottom/换行/缩进）、
  数字与日期显示格式（千分位/百分比/货币/[Red] 负数/中文日期/时分秒分钟-月份消歧/AM-PM/
  [h] 经时/科学计数）。不还原：批注/浮动图片/图表/条件格式/冻结窗格/富文本局部样式。
  上限熔断：单 sheet 3000 行/120 列/12 万格/4MB 输出封顶（截断后表尾提示"下载原件查看全部"），
  源文件 >15MB 直接 failed 回退 SheetJS。
- **`routes.py`**：`PDF_SOURCE_EXTS` 收窄为 `{doc,docx}`、新增 `XLSX_SOURCE_EXTS={xls,xlsx}`；
  `html_status/html_size/html_error` 异步渲染管线（独立 `_HTML_POOL` 1 线程，与 LibreOffice
  长任务分池；启动补偿 `_recover_pending_htmls`；failed 不自动重试）；
  新端点 `GET /files/<id>/preview`、`POST /files/<id>/preview-retry`（管理员）；
  删除清理扩为四件套；`/status` 报 `xlsx_render/xlsx_preview`；
  `_migrate_files` 幂等补 html 三字段 + 历史 Excel 记录 `pdf_status∈{pending,failed}` 复位 none。
- **前端**：`reader.js` 新增 `renderSheetHtml`（/preview → DOMPurify → sheet 页签 + 注入 +
  TSV 复制；拉取失败自动回退 SheetJS）；分派改为 Excel×html_ready→表格预览、
  Word×pdf_ready→PDF.js、其余按扩展名降级；`app.js` 新增
  `isSheetFile/previewStatus/previewPending/previewReady` 辅助，轮询/角标/提示条/上传
  toast 双轨适配（Word=PDF、Excel=表格）；`style.css` 增 `.kb-xlsx-table`（默认网格线 +
  兜底字体，样式主体由单元格 inline style 承载）；版本号 v21/v5/v15（F-2）。
- **文档同步**：插件 README（Excel 表格预览章节/四件套表/接口/已知限制）、根 README
  （插件一览/API 表 +preview +preview-retry/四件套注）、`docs/插件库优化方案-设计文档.md`
  （阶段 7 + §13 决策）、`docs/知识库插件-设计文档.md`（阶段 8）、requirements.txt 注释、
  `.gitignore`（backend/out/ 测试产物）、HANDOFF（状态头/§2/§3/§7.5）。

### 踩坑（本次实测）
1. **Excel m/mm 上下文**：`h:mm` 里的 `mm` 是分钟不是月份——判定标志 `prev_h` 会被
   `:` 等字面量 token 打断；字面量必须**保持**上下文，且"下一个有意义的日期 token"
   要跳过引号与普通字面量再判（`mm分ss秒` 的 mm 应为分钟）。
2. **日期 token 正则漏 `dd`**：`dddd|ddd|` 后直接接 `d`，`dd` 被拆成两个 `d` 渲染出 `22`。
3. **ddd/dddd 是星期不是"日"**：fields 里 `d`=日、`dw`=weekday 必须分开存，否则
   `ddd` 拿 day-of-month 当索引直接 IndexError。
4. **`[h]` 经时**：token 判断要用去掉方括号的 inner 比较（`"[h]"` 不匹配 `^h+$`）；
   经时格式下 `m/mm` 恒为分钟（无月份概念）。
5. **尾逗号千分缩放的 lambda 闭包**：`re.sub(r",+$", lambda m: (scale:=scale+1)...)` 的
   walrus 落在 lambda 作用域，外层 scale 恒 0——改用 search+切片。
6. **openpyxl 新建簿的主题是 Office 2007**（accent1=4F81BD），与 Excel 2016 默认
   （4472C4）不同——测试断言别按后者写；tint 的 HLS 换算与 Excel 实测一致（黑 +0.5=7F7F7F）。
7. 测试期望值坑：`#,##0,` 千分缩放对 1234.5 显示 `1` 是 Excel 正确行为；字符串值不受
   number_format 影响；行高 24（pt）→ 32（px）是 4/3 换算。

### 验证结果
- 格式化矩阵 40+ 用例全过（数字/日期/时间/经时/货币/[Red]/科学计数/舍入/补位）。
- Flask test_client 集成（假 admin 会话 + 临时包加载）：上传 xlsx → `html_status=pending`
  → 后台渲染 ok（34.8KB）→ `/preview` 200 返回 4 sheet → 不存在 id 404 → 删除后
  落盘目录无残留（四件套清理生效）。
- 浏览器目检（IAB 1280×900，`backend/out/sample.html` 汇聚样例）：合并标题蓝底白字居中、
  表头双线底边、`-¥50,400.00` 红字、¥ 千分位、中文日期、百分比、隐藏行(5)/列(H)正确跳过、
  theme+tint 填充与灰字、`9:05 AM`/`15:42:10`/`[h]:mm` 31:25、关网格线 sheet、
  wrap_text 长文本折行不溢出、超宽表 120 列封顶 + 黄色截断提示条——全部符合预期。
- 未做：真机 LibreOffice 环境回归（本轮不涉及 Word 链路，无影响）；真实大表（>15MB）压测
  （熔断逻辑为代码路径审查 + 小样验证）。

### 断点/后续
- git 提交等用户确认（与历史条目口径一致：本轮改动全部未提交）。
- 自测脚本留存 `backend/test_xlsx_render.py`（纯模块级）与
  `backend/test_routes_preview.py`（Flask 集成），改渲染逻辑后可直接重跑回归。
