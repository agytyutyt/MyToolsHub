# JZToolsHub 插件设计规范

| 文档属性 | 内容 |
| --- | --- |
| 文档版本 | v1.1（2026-08-26） |
| 适用范围 | JZToolsHub 全部插件的设计、开发、打包、分发、安装与运维 |
| 目标读者 | 插件开发者、部署运维人员、站点管理员 |
| 关键词约定 | **必须**（MUST）/ **禁止**（MUST NOT）/ **应当**（SHOULD）/ **可以**（MAY），语义遵循 RFC 2119 |
| 变更记录 | v1.1：新增封装型（B 型）插件接入细则——3.1 接入形式、4.2 B 型结构、8.6–8.10 桥接契约、12.3 安装流程、附录 D 最小模板 |

---

## 目录

1. [设计理念：一切皆插件](#1-设计理念一切皆插件)
2. [核心概念与术语](#2-核心概念与术语)
3. [插件分类](#3-插件分类)
4. [插件包目录结构规范](#4-插件包目录结构规范)
5. [manifest.json 清单规范](#5-manifestjson-清单规范)
6. [注册配置规范（config/tools.json）](#6-注册配置规范configtoolsjson)
7. [前端开发规范](#7-前端开发规范)
8. [后端开发规范](#8-后端开发规范)
9. [数据持久化与数据归属规范](#9-数据持久化与数据归属规范)
10. [安全规范](#10-安全规范)
11. [插件生命周期](#11-插件生命周期)
12. [打包后零代码接入流程](#12-打包后零代码接入流程)
13. [版本与兼容性规范](#13-版本与兼容性规范)
14. [质量自检清单](#14-质量自检清单)
15. [附录A：纯前端插件最小模板](#附录a纯前端插件最小模板)
16. [附录B：带后端插件最小模板](#附录b带后端插件最小模板)
17. [附录C：反模式清单（禁止事项）](#附录c反模式清单禁止事项)

---

## 1. 设计理念：一切皆插件

JZToolsHub 的核心架构承诺是：**主框架只提供运行底座，一切业务功能皆为插件**。

### 1.1 框架与插件的职责边界

| 角色 | 职责 | 形态 |
| --- | --- | --- |
| 主框架 | 首页渲染、工具外壳页（iframe 宿主）、插件静态资源服务、配置聚合 API、访问日志、登录会话 | `app.py` + `static/` + `config/`，**一经打包不再修改** |
| 插件 | 一切业务功能：工具页面、业务后端接口、私有数据、私有配置 | `plugins/<id>/` 自包含目录 |

管理后台（`admin`）本身也是一个插件——这是"一切皆插件"的极致体现：连登录鉴权、权限控制这样的基础设施都不在框架里。

### 1.2 四条铁律

1. **框架零改动**：新增 / 下线 / 升级任何功能，都**必须**通过"放入插件目录 + 修改配置"完成，**禁止**修改 `app.py`、`static/` 或其他插件。
2. **目录即插件**：一个插件就是一个自包含目录，**必须**可整体复制、整体删除、跨项目迁移，不依赖外部状态。
3. **配置即启停**：功能是否上架、叫什么名字、归入哪个分类、排在第几位，全部由 `config/tools.json` 决定，**必须**支持不重启即改文案 / 启停。
4. **隔离即安全**：前端以 iframe 物理隔离，后端以独立模块前缀隔离，数据以独立目录隔离；插件之间**禁止**直接引用对方的内部实现。

### 1.3 本规范要达成的目标

> **项目打包部署之后，任何人都能在不写一行代码、不改一次框架的前提下，为本站点接入新功能。**

接入动作被压缩为三个非编码步骤：

```
① 拷贝插件包 → plugins/ 目录
② 在 config/tools.json 登记一条注册信息
③ 刷新浏览器（纯前端）或重启服务进程（含后端）
```

接入物既可以是**源码插件包**（源码型 · A 型），也可以是**封装为独立程序的插件包**（封装型 · B 型，如 Go/Rust/C#/冻结版 Python 编译产物）——两者共用同一套登记与验收口径（第 12 章），详见 3.1 与 8.6。

---

## 2. 核心概念与术语

| 术语 | 定义 |
| --- | --- |
| **插件 ID** | 插件的唯一标识，匹配 `^[A-Za-z0-9_-]+$`，同时是目录名、路由前缀、权限点的主键 |
| **插件包** | `plugins/<id>/` 下可分发的自包含目录，含清单、前端资源及可选后端 |
| **清单（manifest.json）** | 插件根目录下的自描述文件：图标、主题色、入口页、能力标签 |
| **注册表（config/tools.json）** | 站点级配置：站点信息、分类定义、工具注册条目（名称/描述/分类/排序/启停） |
| **外壳页（tool.html）** | 框架提供的 iframe 宿主页，URL 为 `/tool/<id>` |
| **入口页** | 清单中 `entry` 指定的 HTML 文件，经 `/plugin/<id>/<entry>` 对外提供 |
| **权威来源** | 展示用名称 / 描述以 `config/tools.json` 为准，清单中同名字段仅为回退兜底 |
| **核心插件** | `admin`：始终加载的基础设施插件，不随 `enabled` 启停 |
| **源码型插件（A 型）** | 以明文源码交付、由框架动态加载的插件 |
| **封装型插件（B 型）** | 业务逻辑封装为独立可执行程序、经标准适配器桥接接入的插件 |
| **适配器（shim）** | B 型包内由官方 SDK 提供的标准 `routes.py`：负责拉起内部服务、注入身份、反向代理与健康监控，不含业务逻辑 |
| **内部服务** | B 型插件的可执行程序本体，仅监听回环地址、只接受适配器转发的请求 |
| **runtime.json** | B 型插件的运行时声明：协议版本、启动方式、健康检查、超时等（见 8.7） |
| **jz-runtime 协议** | 适配器 ↔ 内部服务的桥接契约，当前版本 `jz-runtime-1`（见 8.8 / 8.9） |

---

## 3. 插件分类

按形态分为三类，复杂度递增：

| 类型 | 代号 | 组成 | 典型场景 | 生效方式 |
| --- | --- | --- | --- | --- |
| 纯前端插件 | L1 | manifest + frontend | 编解码、格式化、计算器等浏览器内完成的工具 | 复制目录后**刷新浏览器即生效，无需重启** |
| 前后端一体插件 | L2 | manifest + frontend + backend | 文件处理、大模型调用、多人协作等需要 Python 能力的功能 | 复制目录后**需重启服务进程** |
| 核心基础设施插件 | C | 同 L2，且 `hidden: true` | 登录鉴权、后台管理（当前仅 `admin`） | 始终加载，不受启停影响 |

**选型原则**：能用 L1 解决的不上 L2；L2 中超过 3 秒的长耗时操作必须走异步任务模式（见 8.5）。

### 3.1 接入形式：源码型（A 型）与封装型（B 型）

在形态（L1/L2/C）之外，插件按**交付物形式**分为两种接入形式：

| 维度 | 源码型（A 型） | 封装型（B 型） |
| --- | --- | --- |
| 交付物 | 明文源码目录 | 独立可执行程序 + 标准适配器 |
| 业务逻辑位置 | `backend/routes.py` 及同包模块 | 外部进程内部（黑盒，站点不可见） |
| 技术栈 | Python 3.8 + Flask | 不限（Go / Rust / C# / Node / 冻结版 Python 等均可） |
| 适用场景 | 内部自研、需评审定制的功能 | 第三方成品工具、闭源商业交付、非 Python 技术栈复用、知识产权保护 |
| 安装差异 | 解压即用 | 增加平台匹配、可执行授权、完整性校验三步 |
| 生效方式 | L1 刷新即生效 / L2 重启 | 重启服务进程 |
| 框架改动 | 无 | **同样为零**（经适配器桥接，框架视角与 A 型同构） |

组合关系：

| 组合 | 说明 |
| --- | --- |
| L1 × A | 纯前端静态资源直接交付 |
| L2 × A | 业务逻辑以 Python 源码随包交付（见 8.2–8.5） |
| L2 × B | 业务逻辑封装为独立程序，适配器桥接接入（见 8.6–8.10） |
| C 型 | 仅允许 A 型——基础设施必须可审计 |

> B 型的价值：部署者拿到的是"开箱即用的黑盒程序"，接入动作仍是零代码的复制 + 登记；插件作者可以用任何擅长的语言开发，并以编译产物保护知识产权。两种形式遵循同一套 manifest / 注册 / 验收体系，可随时互相替换，对框架完全透明。

---

## 4. 插件包目录结构规范

### 4.1 标准结构（源码型 · A 型）

```
plugins/<plugin-id>/            # ★ 目录名 = 插件 ID
├── manifest.json               # 【必须】清单（见第 5 章）
├── frontend/                   # 【必须】前端资源根目录
│   ├── index.html              #   入口页（entry 默认值）
│   ├── app.js                  #   其余资源自由组织，
│   └── style.css               #   内部一律相对路径引用
└── backend/                    # 【可选】Python 后端包（L2 型必须有）
    ├── __init__.py             # 【必须·若有 backend】可为空，仅标识 Python 包
    ├── routes.py               # 【必须·若有 backend】必须导出 register(app)
    ├── helper.py               # 私有模块，同包内相对导入
    ├── requirements.txt        # 【应当】第三方依赖声明（供部署机安装）
    ├── config.json             # 运行配置（含密钥时【必须】gitignore）
    └── data/                   # 运行期用户数据（【必须】gitignore）
```

### 4.2 封装型结构（B 型）

在 A 型基础上，业务后端替换为「标准适配器 + 内部服务」：

```
plugins/excel-pro/              # ★ 封装型（B 型）示例
├── manifest.json               # form 建议填 "package"，其余字段同 A 型
├── frontend/                   # 同 A 型，页面内 fetch('/api/excel-pro/...')
└── backend/
    ├── __init__.py             # 空文件
    ├── routes.py               # ★ 标准适配器（官方 SDK 原样提供，禁止塞业务逻辑）
    ├── runtime.json            # ★ 运行时声明（协议/启动/健康检查，见 8.7）
    ├── requirements.txt        # 仅适配器自身依赖（通常只有 requests）
    ├── SHA256SUMS              # ★ 完整性校验清单（文件名 → SHA256）
    ├── bin/                    # ★ 封装后的可执行程序（按平台分目录）
    │   ├── win-amd64/
    │   │   └── excel-pro-service.exe
    │   └── linux-amd64/
    │       └── excel-pro-service
    ├── config.json             # 运行配置（含密钥时【必须】gitignore）
    └── data/                   # 运行期用户数据（【必须】gitignore，内部服务共用）
```

### 4.3 结构规则

| 编号 | 规则 |
| --- | --- |
| S-1 | 插件的一切文件**必须**位于自身目录内；**禁止**写入 `static/`、`config/`、`logs/`、其他插件目录或框架任意位置。 |
| S-2 | `frontend/` 与 `backend/` 的位置与命名**必须**严格遵守 4.1 / 4.2——框架按此路径做静态映射与动态加载，无任何配置可改。 |
| S-3 | `backend/data/` 与含密钥的 `config.json` **必须**加入 `.gitignore`；分发插件包时应剔除这两类内容或提供空模板。 |
| S-4 | 插件包内**禁止**携带 `.pyc`、`__pycache__`、虚拟环境、node_modules 等构建产物（B 型插件 `bin/` 下的成品程序属于正式交付物，不在此列）。 |
| S-5 | 插件包**应当**附带一份简短 README，说明功能、依赖、配置方法与已知限制。 |
| S-6 | B 型插件**必须**包含合法的 `runtime.json` 与 `SHA256SUMS`；适配器应为官方 SDK 标准文件（附录 D），除 `API_PREFIX` 外**禁止**改动。 |
| S-7 | 多平台发布的 B 型包**应当**按 `bin/{os}-{arch}/` 目录组织，`runtime.json` 以 `{platform}` 占位引用；未覆盖当前平台的包**必须**报出明确错误而非晦涩堆栈。 |
| S-8 | B 型分发物**应当**附第三方组件清单与许可证说明；闭源交付时同时提供接口文档。 |

---

## 5. manifest.json 清单规范

### 5.1 字段定义

| 字段 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `id` | ✅ | — | 插件 ID，**必须**与目录名一致，仅允许 `[A-Za-z0-9_-]` |
| `name` | — | 同 `id` | 展示名**回退值**；权威取值在 `config/tools.json` |
| `description` | — | 空 | 说明**回退值**；同上 |
| `icon` | — | `🧩` | 首页卡片图标（Emoji 单字符为宜） |
| `accent` | — | `#4285F4` | 卡片主题色，Hex 格式 |
| `entry` | — | `index.html` | 入口页路径，相对于 `frontend/` |
| `features` | — | `[]` | 卡片能力标签，字符串数组，建议 ≤ 4 个 |
| `form` | — | `"source"` | 接入形式标识：`source`=A 型（源码），`package`=B 型（封装）；信息性字段，供人工与运维脚本识别（当前框架不消费） |
| `version` | — | `"0.0.0"` | 插件自身版本号，供人工识别与升级比对（见第 13 章） |
| `author` | — | 空 | 作者 / 出品方信息 |

> 框架对未知字段不做校验、直接忽略，因此扩展字段不会破坏加载；但只有上表字段会被框架消费。

### 5.2 示例

```json
{
  "id": "excel-splitter",
  "version": "1.0.0",
  "author": "JZToolsHub 团队",
  "icon": "📊",
  "accent": "#34A853",
  "entry": "index.html",
  "features": ["Excel 拆分", "批量导出"]
}
```

### 5.3 规则

| 编号 | 规则 |
| --- | --- |
| M-1 | `manifest.json` **必须**是合法 JSON（无注释、无尾逗号、UTF-8 无 BOM），否则整个插件无法被发现。 |
| M-2 | `name` / `description` 写入清单仅作为"把插件目录拷到未登记的新项目时的兜底"；两处同时存在时，**以 `config/tools.json` 为权威**，不建议两处维护不同文案。 |
| M-3 | `id` 一经发布**禁止**变更——它是静态路由、API 前缀、日志标签、用户权限点的共同主键。 |

---

## 6. 注册配置规范（config/tools.json）

### 6.1 配置结构

```jsonc
{
  "site": {                       // 站点信息（框架消费）
    "title": "...",
    "subtitle": "...",
    "footer": "...",
    "logging": true               // 访问日志开关
  },
  "categories": [                 // 分类定义
    { "id": "dev", "name": "开发者工具", "enabled": true }
  ],
  "tools": [                      // 工具注册条目（每插件一条）
    {
      "id": "excel-splitter",
      "name": "Excel 拆分器",      // ★ 权威展示名
      "description": "一句话描述", // ★ 权威描述
      "category": "dev",          // 引用 categories.id
      "enabled": true,            // 总开关：false = 卡片隐藏 + 后端不加载
      "hidden": false,            // true = 只注册后端、不出卡片（核心插件用）
      "order": 6                  // 排序权重，数值越小越靠前
    }
  ]
}
```

### 6.2 工具注册条目字段说明

| 字段 | 必填 | 缺省行为 | 说明 |
| --- | --- | --- | --- |
| `id` | ✅ | — | 与插件目录名、manifest.id 三者一致（硬约束） |
| `name` | — | 回退 manifest.name，再回退 id | 权威展示名，改这里即时生效 |
| `description` | — | 回退 manifest.description | 权威描述 |
| `category` | ✅ | 显示为"未分类" | 分类 ID；分类不存在时同样落入"未分类" |
| `enabled` | — | `true` | `false` 时卡片隐藏**且后端不注册**，无需删代码 |
| `hidden` | — | `false` | `true` 时不出首页卡片但正常注册后端 |
| `order` | — | `0` | 同分类内升序排列 |

### 6.3 注册规则

| 编号 | 规则 |
| --- | --- |
| R-1 | 每个待上架插件**必须**在 `tools` 数组中有且仅有一条注册记录；重复 ID 的行为未定义。 |
| R-2 | 新增分类**应当**同步在 `categories` 中声明；分类也支持 `enabled: false` 整组下线。 |
| R-3 | 名称 / 描述 / 排序 / 启停等纯配置变更**保存后刷新页面立即生效，无需重启**。 |
| R-4 | 新增 L2 型插件的**后端路由**在服务启动时一次性注册，因此首次安装或更换后端代码后**必须重启服务进程**（开发模式 debug=True 由自动重载机制代劳）。 |
| R-5 | 用户权限联动：非超管账号仅能访问其 `permissions` 列表内的插件 ID；新插件上架后由管理员在「人员管理 → 权限」浮窗中按人授权。 |
| R-6 | 封装型（B 型）插件的内部服务不进入注册表、不单独授权——权限点仍以插件 ID 为唯一粒度，鉴权统一发生在框架层与适配器层。 |

---

## 7. 前端开发规范

### 7.1 运行模型

入口页以 iframe 嵌入外壳页 `/tool/<id>` 加载，src 为 `/plugin/<id>/<entry>`。每个插件运行在独立 iframe 中，与框架及其他插件天然隔离。

### 7.2 条款

| 编号 | 级别 | 规则 |
| --- | --- | --- |
| F-1 | 必须 | 页面为普通静态网页；引用同目录资源**一律使用相对路径**（`./app.js`、`css/style.css`）。iframe 的 src 前缀决定了绝对路径会指错位置。 |
| F-2 | 必须 | **静态资源变更必须递增版本号**：框架对 `/plugin/` 下 `.js/.css/.png` 等下发 1 天强缓存，而 `.html` 为 no-cache。因此每次修改 JS/CSS 后，必须把入口页中的引用改为 `app.js?v=N+1`（N 递增），否则用户将长期看到旧文件。 |
| F-3 | 必须 | 所有用户输入在插入 DOM 前**必须**做 XSS 转义（或统一用 `textContent` 渲染）。 |
| F-4 | 必须 | API Key、地图 Key 等敏感配置**禁止**硬编码在前端源码或提交进仓库；应当在插件页面内提供配置面板，保存到 localStorage，或调用本插件后端的 config 接口落盘到 `backend/config.json`。 |
| F-5 | 应当 | 遵循站点的 Material Design 风格（参考既有插件的布局与配色）；`accent` 色应与页面主色调一致。 |
| F-6 | 应当 | 不依赖框架全局变量、不尝试操作 parent 文档或兄弟 iframe；确需向框架反馈的场景未来应走约定的 postMessage 通道（当前无此需求）。 |
| F-7 | 可以 | 使用 CDN 引入第三方前端库；离线内网部署的插件应将库文件放进自己的 `frontend/`。 |

---

## 8. 后端开发规范

### 8.1 加载模型

框架启动时扫描各插件的 `backend/routes.py`，以独立模块名 `jztools_<id>` 动态加载并调用其中的 `register(app)`。插件间符号互不可见、互不污染。`register(app)` 因此成为唯一的后端挂载点——无论业务逻辑是随包源码（A 型），还是位于适配器背后的独立程序（B 型，见 8.6）。

### 8.2 最小契约

```python
# plugins/<id>/backend/routes.py
from flask import jsonify

API_PREFIX = "/api/excel-splitter"

def register(app):
    """框架启动时自动调用；app 为 Flask 实例。"""
    @app.get(f"{API_PREFIX}/ping")
    def ping():
        return jsonify({"ok": True})
```

### 8.3 条款

| 编号 | 级别 | 规则 |
| --- | --- | --- |
| B-1 | 必须 | `routes.py` **必须**导出 `register(app)`；缺失则该后端静默不加载。 |
| B-2 | 必须 | 所有路由**必须**挂在 `/api/<id>/...` 前缀下，防止多插件路由冲突。 |
| B-3 | 必须 | 包内私有模块**必须**使用相对导入（`from . import helper`），不得依赖全局包名。 |
| B-4 | 必须 | 第三方依赖**应当**写入 `requirements.txt` 并提供依赖检查接口 `GET /api/<id>/status`，返回各依赖可用性；缺依赖时优雅降级（给出提示），**禁止**让整个插件 500 或拖垮主进程。 |
| B-5 | 应当 | 插件自身的可调参数（抽样间隔、阈值等）通过 `GET/POST /api/<id>/config` 读写，持久化到 `backend/config.json`。 |
| B-6 | 禁止 | **禁止**修改 Flask 应用全局行为（全局 before_request、errorhandler 等）——那是 admin 核心插件的专属职责。 |
| B-7 | 禁止 | **禁止** import 其他插件的后端模块；唯一例外是 admin 暴露的会话工具 `get_session_user` 与日志辅助 `set_operation`（见 9.3 标准样板）。 |
| B-8 | 禁止 | **禁止**在插件内自行落盘访问日志——框架已统一异步记录，`/api/<id>/...` 会自动带上功能标签；如需更具体的操作描述，可在处理器内调用 `set_operation("描述")` 标记（从 `jztools_admin.routes` 导入，导入失败兜底为空操作）。 |

### 8.4 数据读写接口约定

涉及用户数据的插件，接口**应当**遵循 REST 习惯并保持与既有插件一致的返回风格：

- 错误统一返回 JSON `{"error": "..."}` + 合适的状态码；
- 「存在但无权访问」的资源返回 **404**（不暴露存在性），操作被拒返回 **403**；
- 未登录调用一律 401（框架兜底，插件不必重复实现）。

### 8.5 异步任务模式（长耗时操作必须遵守）

预计超过 3 秒的处理（大模型调用、视频转码、大文件解析等）**必须**采用「提交即返回、轮询取结果」模式：

```text
POST /api/<id>/<task>          → 校验入参，立即返回 {"task_id": "..."} （毫秒级）
GET  /api/<id>/result/<task_id>→ {"status": "pending|running|done|error", ...}
```

实现要点：

1. 用有界线程池执行（如 `ThreadPoolExecutor(max_workers=2)`），**禁止**占用 HTTP worker 执行长任务；
2. 任务表加锁保护，结果设置 TTL 自动清理（参考 30 分钟），防止内存泄漏；
3. 提交前做前置校验（如 API Key 是否已配置），避免无效排队。

### 8.6 封装型插件（B 型）接入总则

B 型插件 = **标准适配器 + 封装后的内部服务**。对框架而言它与 A 型完全同构——同样的 `register(app)`、同样的 `/api/<id>/` 前缀、同样的启停语义，因此**框架依旧零改动**：

```
浏览器 ─▶ /plugin/<id>/...   ─▶ frontend/（静态资源，与 A 型完全一致）
浏览器 ─▶ /api/<id>/...      ─▶ 适配器 routes.py ──HTTP──▶ 内部服务 bin/*
                                     │ 拉起 · 健康检查 · 身份注入 · 反向代理 · 监控
```

| 编号 | 级别 | 规则 |
| --- | --- | --- |
| B-9 | 必须 | 适配器只承担桥接职责（拉起、代理、监控、身份注入），**禁止**在其中编写业务逻辑；业务逻辑一律在内部服务内实现。 |
| B-10 | 必须 | 内部服务技术栈不限，但必须实现 `jz-runtime-1` 协议契约（8.8 / 8.9）。 |
| B-11 | 必须 | 适配器**必须**采用官方 SDK 标准模板（附录 D），除 `API_PREFIX` 与清单引用外不做定制，保证可随 SDK 统一升级。 |
| B-12 | 禁止 | **禁止**绕过适配器把内部服务端口暴露给回环以外的网络（见 SEC-7）。 |

### 8.7 runtime.json 运行时声明

B 型插件**必须**在 `backend/runtime.json` 中声明运行时参数：

| 字段 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- |
| `protocol` | ✅ | — | 协议版本，当前 `"jz-runtime-1"` |
| `mode` | ✅ | — | `http-service`（常驻服务）或 `cli`（无状态命令行） |
| `executable` | ✅ | — | 可执行文件相对路径，支持 `{platform}` / `{ext}` 占位符 |
| `args` | — | `[]` | 启动参数模板，支持 `{port}` 占位符 |
| `lazy` | — | `true` | `true`=首个请求时拉起内部服务；`false`=register 时立即拉起 |
| `health_path` | — | `/healthz` | HTTP 模式健康检查路径 |
| `startup_timeout_s` | — | `15` | 健康检查等待上限，超时判定启动失败 |
| `idle_shutdown_s` | — | `0` | 空闲回收秒数，0=常驻 |
| `cli_timeout_s` | — | `60` | CLI 模式单次执行超时 |

示例：

```json
{
  "protocol": "jz-runtime-1",
  "mode": "http-service",
  "lazy": true,
  "executable": "bin/{platform}/excel-pro-service{ext}",
  "args": ["--port={port}"],
  "health_path": "/healthz",
  "startup_timeout_s": 15
}
```

> `{platform}` 由适配器按运行环境解析为 `win-amd64` / `linux-amd64` / `darwin-arm64` 等；`{ext}` 在 Windows 解析为 `.exe`，其余平台为空串。

### 8.8 HTTP 服务模式细则（mode=http-service）

适配器生命周期行为：

1. **拉起**：按 `lazy` 决定时机；从回环地址自动分配空闲端口（**禁止**写死固定端口），经环境变量 `JZ_PLUGIN_PORT` 与参数占位符 `{port}` 双通道传入。
2. **环境变量契约**：`JZ_PLUGIN_HOME`（插件根目录）、`JZ_PLUGIN_PORT`、`JZ_PLUGIN_DATA`（=`backend/data/`）、`JZ_PLUGIN_CONFIG_FILE`（=`backend/config.json`）。内部服务的持久化与配置**只能**落在这两个路径。
3. **健康等待**：轮询 `GET {health_path}` 直至 2xx 或超时；失败时在 `/api/<id>/status` 上报具体原因（可执行缺失 / 秒退 / 健康超时）。
4. **身份注入**：代理转发时剥离 Cookie 等凭据，改注入受信身份头：

| 注入头 | 含义 | 匿名时 |
| --- | --- | --- |
| `X-JZ-User` | 登录名 | 不注入 |
| `X-JZ-Display-Name` | 姓名 | 不注入 |
| `X-JZ-Unit-Id` / `X-JZ-Department-Id` | 单位 / 部门 ID | 注入空串 |
| `X-JZ-Super-Admin` | `true` / `false` | 注入 `false` |

5. **代理规则**：透传方法、查询串与请求体；剔除逐跳头（Connection / Transfer-Encoding 等）；响应原样回传（支持流式大文件）；适配器侧统一超时 120 秒。
6. **日志**：子进程 stdout/stderr 重定向至 `backend/data/service.log`。
7. **停止**：主进程退出时 atexit 终止子进程；`idle_shutdown_s > 0` 时空闲回收。
8. **崩溃策略**：5 分钟内最多自动重启 3 次，超出进入熔断态；`/api/<id>/status` 如实报告 `degraded`，直至人工重启服务进程。

内部服务契约（`jz-runtime-1`）：

| 项 | 要求 |
| --- | --- |
| 监听 | 仅 `127.0.0.1:$JZ_PLUGIN_PORT` |
| 健康 | `GET /healthz` 返回 2xx |
| 身份 | 不解析 Cookie/Session；仅信任回环请求携带的 `X-JZ-*` 头；需要登录态而头缺失时自行返回 401 |
| 数据 | 持久化仅限 `$JZ_PLUGIN_DATA`，配置仅限 `$JZ_PLUGIN_CONFIG_FILE` |
| 退出 | 收到终止信号后 ≤ 5 秒退出 |
| 错误 | JSON `{"error": "..."}` + 4xx/5xx，与 8.4 一致 |

| 编号 | 级别 | 规则 |
| --- | --- | --- |
| B-13 | 必须 | 内部服务**禁止**读取或依赖浏览器 Cookie / Session。 |
| B-14 | 必须 | 适配器转发前**必须**剥离 Cookie 头并按上表注入身份头；匿名请求不注入用户类头。 |
| B-15 | 应当 | 当前框架按单实例运行内部服务，内部服务不应假设可与兄弟实例并行共享同一 `data/` 目录。 |
| B-16 | 应当 | 长任务遵循 8.5 异步任务模式；task_id 轮询在内部服务内部实现，经适配器透传即可。 |
| B-17 | 禁止 | **禁止**在 `args` 中引入未经 `runtime.json` 声明的动态字符串拼接。 |

### 8.9 CLI 模式细则（mode=cli）

适用于无状态、单次完成的转换 / 计算类工具：

| 编号 | 级别 | 规则 |
| --- | --- | --- |
| B-18 | 必须 | 以**参数列表**方式调用（等价 `subprocess.run(list)`），**禁止** shell 字符串拼接（SEC-10）。 |
| B-19 | 必须 | 请求体原文经 stdin（UTF-8）传入；退出码 0 → 200 + stdout 中的 JSON 作为响应；非 0 → 500 + stderr 摘录（截断）。 |
| B-20 | 必须 | 执行超过 `cli_timeout_s` 强制终止并返回 504；输入超过 10MB 直接拒绝。 |
| B-21 | 应当 | 有状态、高并发或需长连接的工具**禁止**使用 CLI 模式，应改用 http-service。 |

### 8.10 封装型安全条款

| 编号 | 级别 | 规则 |
| --- | --- | --- |
| SEC-7 | 必须 | 内部服务**必须**且只能监听 `127.0.0.1`；绑定 `0.0.0.0` 的交付物一律拒收。 |
| SEC-8 | 必须 | 分发**必须**附 `SHA256SUMS`；安装方校验失败**必须**拒绝上线并给出明确错误。 |
| SEC-9 | 必须 | 身份头信任边界 = 回环地址：内部服务仅信任来自 `127.0.0.1` 的请求及其携带的 `X-JZ-*` 头，其他来源一律拒绝。 |
| SEC-10 | 必须 | 调用子进程一律参数列表化，**禁止** `shell=True` / 字符串拼接命令行。 |
| SEC-11 | 应当 | 内部服务以普通用户权限运行，不要求管理员/root；Windows 防火墙、SmartScreen 放行步骤写入插件 README。 |

---

## 9. 数据持久化与数据归属规范

### 9.1 存储位置

凡插件产生的用户数据，**必须**存放在 `plugins/<id>/backend/data/` 下，并 gitignore。一记录一文件或单库文件均可，但同一目录混合多种用途时，扫描逻辑**必须**能按结构特征过滤出目标文件（历史教训：类别库文件曾被误当作业务记录读入）。

### 9.2 归属两条铁律

凡是插件持久化"用户产生的数据"，**必须**遵守：

> **铁律一（落盘带归属）**：记录**必须**包含 `created_by` / `created_by_name` / `unit_id` / `department_id` 四个字段，创建时从会话取得，**禁止**从请求体接收（防伪造归属）。

> **铁律二（读取必过滤）**：任何返回这些数据的接口，第一步取会话用户（取不到即 401），第二步按可见性规则过滤后才允许序列化返回；单条读写遵循「读 = 可见性规则、写 = 本人或超管」。

### 9.3 可见性规则与标准样板

可见性判定（记当前用户为 U，一条数据为 X）：

| 场景 | 可见条件（满足其一） |
| --- | --- |
| 单位级数据 | U 是超管，或 `X.unit_id == u(U)` |
| 部门级数据 | U 是超管，或 `X.department_id == d(U)` 且 `X.unit_id == u(U)` |
| 私人数据 | U 是超管，或 `X.created_by == n(U)` |
| 本人视图列表 | U 是超管，或 `X.created_by == n(U)` |
| 本部门视图列表 | 本人视图 ∪ 部门级数据 |

标准取用样板（所有需要身份上下文的 L2 插件统一照抄）：

```python
try:
    from jztools_admin.routes import get_session_user
except Exception:
    get_session_user = None


def current_user_or_none():
    if get_session_user is None:
        return None
    return get_session_user()
```

日志辅助（可选，B-8）：需在访问日志中标记具体操作时，同样从 `jztools_admin.routes` 导入 `set_operation`：

```python
try:
    from jztools_admin.routes import set_operation
except Exception:
    def set_operation(op):
        pass

# 处理器内：set_operation("发布公告")
```

写操作约束：重命名 / 删除 / 改归属等管理动作仅限创建者或超级管理员；协作编辑类内容可以放宽为「可见即可编辑」（如共享文档正文保存）。

---

## 10. 安全规范

| 编号 | 级别 | 规则 |
| --- | --- | --- |
| SEC-1 | 必须 | 插件 ID 仅允许 `[A-Za-z0-9_-]`；任何拼路径的场景**禁止**接受 `..`、绝对路径、URL 编码穿越变体（框架已在静态路由层拦截，插件自建下载接口同样要遵守）。 |
| SEC-2 | 必须 | 密钥、Token、真实 Key **禁止**硬编码、禁止入库（git）、禁止打印进文档与日志。运行密钥放 `backend/config.json`（gitignore）或由用户在页面内录入。 |
| SEC-3 | 必须 | 上传文件**必须**校验扩展名与大小上限，落盘文件名重命名为服务端生成 ID，**禁止**直接沿用用户上传的原始文件名。 |
| SEC-4 | 必须 | 匿名请求一律拒绝（302 / 401）；插件**禁止**为绕过登录而在自己接口里另开匿名白名单。 |
| SEC-5 | 必须 | 异常响应**禁止**向前端泄漏堆栈、服务器绝对路径等内部细节。 |
| SEC-6 | 应当 | 对外调用第三方 HTTP 服务设置连接与读取超时；对外部输入的解析（正则、JSON、Office 文件）包裹异常处理。 |

---

## 11. 插件生命周期

```
 开发 ──▶ 打包 ──▶ 分发 ──▶ 安装 ──▶ 启用 ──┬─▶ 升级（覆盖目录，回到安装）
                │                           ├─▶ 停用（enabled:false，保留现场）
                └─ 废弃版本归档              └─▶ 卸载（删配置条目 → 备份 data/ → 删目录）
```

| 阶段 | 关键要求 |
| --- | --- |
| 开发 | 按第 4–10 章条款实现；在干净环境跑通第 14 章自检清单。 |
| 打包 | 产出物 = 插件目录整体 zip（剔除 `data/`、`__pycache__`、含密钥的 `config.json`）；附依赖安装说明。B 型另须附 `SHA256SUMS` 与平台标注（`bin/{os}-{arch}/`）。 |
| 分发 | 插件包 + 目标机器的 `config/tools.json` 注册片段，一起交付。 |
| 安装 | 见第 12 章。 |
| 升级 | 覆盖目录前备份旧 `data/` 与 `config.json`；升级含前端改动必须递增 `?v=N`（F-2）；含后端改动必须重启。B 型：替换 `bin/` 与 `SHA256SUMS` 后重启，`data/` 保持原位并确保兼容（V-7）。 |
| 停用 | 仅置 `enabled: false`，保留代码与数据，随时恢复。 |
| 卸载 | 先删注册条目 → 再备份数据 → 最后删目录；**禁止**先删目录（会导致配置残留悬空引用）。 |

---

## 12. 打包后零代码接入流程

本章是本规范的最终交付目标：**站点已打包部署后，接入一个新功能的完整操作手册。全程零代码，只做文件复制与配置编辑。**

> 无论插件是源码包（A 型）还是封装包（B 型），**登记条目与验收口径完全一致**；差异仅在安装动作本身（12.1 / 12.2 / 12.3）。

### 12.1 安装新插件（L1 纯前端型）

| 步骤 | 操作 | 生效 |
| --- | --- | --- |
| 1 | 将插件包解压至 `plugins/excel-splitter/` | — |
| 2 | 编辑 `config/tools.json`：在 `tools` 数组追加一条 `{id, name, description, category, enabled: true, order}`（如需新分类，先在 `categories` 补一条） | — |
| 3 | 浏览器刷新首页 | ✅ 卡片出现，点击即可使用，**无需重启** |

### 12.2 安装新插件（L2 前后端一体型）

| 步骤 | 操作 | 生效 |
| --- | --- | --- |
| 1 | 将插件包解压至 `plugins/<id>/` | — |
| 2 | 若 `backend/requirements.txt` 非空：`pip install -r plugins/<id>/backend/requirements.txt` | — |
| 3 | 编辑 `config/tools.json` 登记（同 12.1 第 2 步） | — |
| 4 | 重启服务进程（`python app.py` 或对应守护方式） | ✅ 后端路由随启动注册完毕 |
| 5 | 刷新首页验证；若插件有依赖，可访问 `GET /api/<id>/status` 确认依赖齐备 | ✅ |

### 12.3 安装封装型插件（L2 · B 型）

| 步骤 | 操作 | 说明 |
| --- | --- | --- |
| 1 | 将插件包解压至 `plugins/<id>/`，确认存在 `backend/runtime.json` 与目标平台的 `bin/{os}-{arch}/` | 平台不符时向发行方索取对应架构的包 |
| 2 | `pip install -r plugins/<id>/backend/requirements.txt` | 仅适配器自身依赖（通常只有 requests） |
| 3 | Linux/macOS：对 `bin/` 下全部文件执行 `chmod +x` | Windows 免 |
| 4 | 按 `SHA256SUMS` 校验完整性（如 `sha256sum -c SHA256SUMS`） | 校验失败立即停止安装（SEC-8） |
| 5 | 编辑 `config/tools.json` 登记（同 12.1 第 2 步） | — |
| 6 | 重启服务进程 | 内部服务按 `lazy` 策略在首次请求或启动时拉起 |
| 7 | 验证：`GET /api/<id>/status` 返回 `internal_service: healthy`；首页卡片可点开可用 | 异常时查看 `backend/data/service.log` |

### 12.4 常见运维动作速查

| 动作 | 操作 | 是否重启 |
| --- | --- | --- |
| 改名称 / 描述 / 图标 / 排序 | 编辑 `config/tools.json`（图标在 manifest） | 否，刷新即生效 |
| 临时下线 | 该条目 `enabled: false` | 否，刷新即生效 |
| 彻底下线某分类 | 分类 `enabled: false` | 否 |
| 恢复上线 | `enabled: true` | 否 |
| 按人授权 | 管理后台「人员管理 → 权限」勾选该插件 ID | 否，重新登录或刷新会话生效 |
| 升级插件 | 覆盖目录 → 前端递增 `?v=` → 有后端改动则重启 | 视改动而定 |
| 卸载 | 见 11 章生命周期表 | 删目录后重启 |
| B 型内部服务起不来 | 查看 `backend/data/service.log` 与 `GET /api/<id>/status` 的 error 字段 | 否（修复后首个请求自动重拉） |
| B 型平台不符（拿错 win/linux 包） | 更换对应 `bin/{os}-{arch}/` 交付物后重启 | 是 |

> 验收口径：安装是否成功，以 `/api/tools` 能看到该工具、首页卡片可点开、外壳页 iframe 正常加载入口页为准。

---

## 13. 版本与兼容性规范

| 编号 | 规则 |
| --- | --- |
| V-1 | 插件**应当**在 manifest 中维护 `version`（语义化 `主.次.修订`）：修订号=缺陷修复；次版本=向后兼容的功能增强；主版本=不兼容变更。 |
| V-2 | 升级**禁止**删除或变更既有 `id`；**禁止**变更既有对外接口的语义；新增字段应当容忍缺省。 |
| V-3 | 插件对 `backend/data/` 中的历史数据**应当**兼容读取；确需迁移时，在 `register(app)` 内做幂等迁移（重复启动不重复迁、不破坏原数据），**禁止**要求用户手工清洗数据。 |
| V-4 | 插件包对框架的最小要求如有特殊依赖（Python 版本、系统组件），**必须**在插件 README 中显式声明。 |
| V-5 | 自包含验收标准：把插件目录 + 一段 `tools.json` 注册片段复制到另一台全新部署的 JZToolsHub 上，按第 12 章操作即可完整工作——达不到即为不合格交付。 |
| V-6 | B 型**必须**随包提供 `SHA256SUMS` 并在 `runtime.json` 声明 `protocol` 版本；适配器遇到不认识的更高协议主版本**必须**拒绝启动并给出明确错误，而不是带病运行。 |
| V-7 | B 型可执行文件升级必须整目录替换并保持 `data/` 兼容；数据结构变更时由内部服务自行完成幂等迁移（同 V-3），**禁止**要求用户清洗数据或重装插件。 |

---

## 14. 质量自检清单

发布 / 交付前逐项打勾：

**结构与清单**

- [ ] 目录名 = manifest.id = tools.json 注册 id，三者一致且匹配 `^[A-Za-z0-9_-]+$`
- [ ] `manifest.json` 为合法 UTF-8 JSON，`entry` 指向的文件真实存在于 `frontend/`
- [ ] 未夹带 `__pycache__` / 构建产物 / 含密钥文件

**前端**

- [ ] 资源全部相对路径引用；本轮改动已递增 `?v=N`（F-2）
- [ ] 用户输入渲染前已转义；无 XSS 注入点
- [ ] 无硬编码 Key；配置面板可用

**后端（L2）**

- [ ] `backend/__init__.py` 存在；`routes.py` 导出 `register(app)`
- [ ] 全部路由挂 `/api/<id>/` 前缀，与他插件无冲突
- [ ] 长耗时操作已异步化（task_id + 轮询），线程池有界，任务结果有 TTL
- [ ] 提供 `/status` 依赖自检；缺依赖优雅降级
- [ ] `requirements.txt` 完整；`pip install -r` 后功能可用

**数据与权限**

- [ ] 用户数据只写 `backend/data/` 且已 gitignore
- [ ] 落盘记录含四个归属字段，来源为会话而非请求体（9.2 铁律一）
- [ ] 列表/详情接口均按可见性规则过滤；越权读 404、越权写 403（铁律二）
- [ ] 数据目录混用时扫描逻辑能过滤非目标文件

**封装型（B 型）附加项**

- [ ] `runtime.json` 为合法 JSON；`executable` 经 `{platform}` / `{ext}` 解析后真实存在
- [ ] `SHA256SUMS` 齐全，在干净安装机上校验通过
- [ ] 内部服务仅监听 `127.0.0.1`；`/healthz` 在超时内可达
- [ ] 经适配器的匿名请求被内部服务拒绝（401）；登录后身份头注入正确
- [ ] 强杀子进程后能按策略恢复或熔断，`/api/<id>/status` 如实反映状态
- [ ] 子进程 stdout/stderr 已落 `backend/data/service.log`
- [ ] 未改动官方 SDK 适配器模板的业务无关部分

**集成演练**

- [ ] 在一份干净的部署副本上完成「拷贝 → 登记 → 验证」全流程
- [ ] `enabled: false` 能干净下线，恢复后功能完好
- [ ] `tools.json` 中 name / description 已填写（权威文案）

---

## 附录A：纯前端插件最小模板

```
plugins/hello/
├── manifest.json
└── frontend/
    └── index.html
```

`manifest.json`

```json
{
  "id": "hello",
  "version": "1.0.0",
  "icon": "👋",
  "accent": "#4285F4",
  "entry": "index.html",
  "features": ["示例"]
}
```

`frontend/index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hello</title>
</head>
<body>
  <h1>你好，JZToolsHub</h1>
  <script src="./app.js?v=1"></script>
</body>
</html>
```

`config/tools.json` 注册片段

```json
{ "id": "hello", "name": "你好工具", "description": "第一个示例插件", "category": "dev", "enabled": true, "order": 99 }
```

## 附录B：带后端插件最小模板

在附录A基础上增加：

```
plugins/hello/
└── backend/
    ├── __init__.py        # 空文件
    ├── routes.py
    └── requirements.txt   # 无第三方依赖时可留空
```

`backend/routes.py`

```python
from flask import jsonify, request

API_PREFIX = "/api/hello"


def register(app):
    @app.get(f"{API_PREFIX}/status")
    def status():
        deps = {}
        try:
            import openpyxl  # noqa: F401  按需替换为真实依赖
            deps["openpyxl"] = True
        except Exception:
            deps["openpyxl"] = False
        return jsonify({"ok": all(deps.values()), "dependencies": deps})

    @app.get(f"{API_PREFIX}/ping")
    def ping():
        return jsonify({"ok": True})

    @app.post(f"{API_PREFIX}/echo")
    def echo():
        data = request.get_json(silent=True) or {}
        return jsonify({"you_said": str(data.get("msg", ""))[:500]})
```

安装：解压 → 登记（同附录A）→ 重启服务 → 访问 `/api/hello/status` 验证。

## 附录C：反模式清单（禁止事项）

| # | 反模式 | 后果 | 正确做法 |
| --- | --- | --- | --- |
| 1 | 修改 `app.py` / `static/` 来加功能 | 破坏"打包后零改动"，升级即冲突 | 做成插件 |
| 2 | 把插件资源放进 `static/` 或别的插件目录 | 迁移即丢失、卸载留残骸 | 一切收敛在 `plugins/<id>/` |
| 3 | 改了 JS/CSS 不递增 `?v=` | 用户看到一天旧缓存 | F-2 版本号纪律 |
| 4 | 后端路由不挂 `/api/<id>/` 前缀 | 多插件路由冲突 | B-2 前缀强制 |
| 5 | 长耗时操作同步阻塞 HTTP worker | 并发拖垮整站 | 8.5 异步任务模式 |
| 6 | 归属字段从请求体接收 / 列表不过滤 | 越权读写他人数据 | 9.2 两条铁律 |
| 7 | API Key 写死进仓库 | 泄漏风险 | SEC-2 配置外置 |
| 8 | 卸载时先删目录后清配置 | 悬空引用报错 | 11 章卸载顺序 |
| 9 | import 其他插件的私有模块 | 插件耦合、卸载连锁崩溃 | B-7，仅允许 admin 会话工具 |
| 10 | 插件自建匿名白名单绕过登录 | 击穿全站鉴权 | SEC-4 |
| 11 | B 型内部服务绑定 `0.0.0.0` | 绕过框架鉴权直接暴露到局域网 | SEC-7，仅回环 |
| 12 | B 型写死固定端口号 | 多插件端口冲突、无法并行部署 | 8.8-1 自动分配空闲端口 |
| 13 | 内部服务解析 Cookie/Session | 破坏信任边界，身份可伪造 | B-13/B-14，仅认 `X-JZ-*` 头 |
| 14 | shell 字符串拼接调用 CLI 程序 | 命令注入 | B-18 / SEC-10 参数列表化 |
| 15 | 在官方适配器里塞业务逻辑 | SDK 升级即失配、职责混乱 | B-9，业务进内部服务 |

---

## 附录D：封装型（B 型）插件最小模板

```
plugins/hello-pro/
├── manifest.json                 # "form": "package"，其余字段同附录 A
├── frontend/
│   └── index.html                # 页面内 fetch('/api/hello-pro/...')
└── backend/
    ├── __init__.py               # 空文件
    ├── routes.py                 # ★ 标准适配器（下方骨架，官方 SDK 原样提供）
    ├── runtime.json              # ★ 运行时声明
    ├── requirements.txt          # requests
    ├── SHA256SUMS
    └── bin/
        ├── win-amd64/hello-pro-service.exe
        └── linux-amd64/hello-pro-service
```

`runtime.json`

```json
{
  "protocol": "jz-runtime-1",
  "mode": "http-service",
  "lazy": true,
  "executable": "bin/{platform}/hello-pro-service{ext}",
  "args": ["--port={port}"],
  "health_path": "/healthz",
  "startup_timeout_s": 15
}
```

`backend/routes.py`（标准适配器骨架）

```python
import atexit
import json
import os
import socket
import subprocess
import threading
import time

import requests
from flask import jsonify, request, Response

try:
    from jztools_admin.routes import get_session_user
except Exception:
    get_session_user = None

PLUGIN_DIR = os.path.dirname(os.path.abspath(__file__))
with open(os.path.join(PLUGIN_DIR, "runtime.json"), encoding="utf-8") as f:
    RT = json.load(f)

API_PREFIX = "/api/hello-pro"
STATE = {"proc": None, "base": None, "error": None}
LOCK = threading.Lock()


def _resolve(rel):
    platform = "win-amd64" if os.name == "nt" else "linux-amd64"
    ext = ".exe" if os.name == "nt" else ""
    return os.path.join(PLUGIN_DIR, rel.replace("{platform}", platform).replace("{ext}", ext))


def _free_port():
    s = socket.socket()
    s.bind(("127.0.0.1", 0))
    port = s.getsockname()[1]
    s.close()
    return port


def _spawn():
    port = _free_port()
    exe = _resolve(RT["executable"])
    if not os.path.isfile(exe):
        raise FileNotFoundError(f"缺少可执行文件：{exe}")
    env = {**os.environ,
           "JZ_PLUGIN_PORT": str(port),
           "JZ_PLUGIN_HOME": PLUGIN_DIR,
           "JZ_PLUGIN_DATA": os.path.join(PLUGIN_DIR, "backend", "data"),
           "JZ_PLUGIN_CONFIG_FILE": os.path.join(PLUGIN_DIR, "backend", "config.json")}
    args = [a.replace("{port}", str(port)) for a in RT.get("args", ["--port={port}"])]
    log = open(os.path.join(PLUGIN_DIR, "backend", "data", "service.log"), "ab")
    STATE["proc"] = subprocess.Popen([exe] + args, cwd=PLUGIN_DIR, env=env,
                                     stdout=log, stderr=subprocess.STDOUT)
    STATE["base"] = f"http://127.0.0.1:{port}"
    deadline = time.time() + RT.get("startup_timeout_s", 15)
    while time.time() < deadline:
        if STATE["proc"].poll() is not None:
            raise RuntimeError("内部服务启动即退出，详见 backend/data/service.log")
        try:
            if requests.get(STATE["base"] + RT.get("health_path", "/healthz"), timeout=1).ok:
                return
        except Exception:
            time.sleep(0.3)
    raise TimeoutError("内部服务健康检查超时")


def ensure_up():
    with LOCK:
        if STATE["error"]:
            raise RuntimeError(STATE["error"])
        if STATE["proc"] and STATE["proc"].poll() is None:
            return
        STATE["error"] = None
        try:
            _spawn()
        except Exception as exc:
            STATE["error"] = str(exc)
            raise


def _identity_headers():
    u = get_session_user() if get_session_user else None
    if not u:
        return {}
    return {"X-JZ-User": u.get("username", ""),
            "X-JZ-Display-Name": u.get("name", ""),
            "X-JZ-Unit-Id": str(u.get("unit_id") or ""),
            "X-JZ-Department-Id": str(u.get("department_id") or ""),
            "X-JZ-Super-Admin": "true" if u.get("super_admin") else "false"}


def register(app):
    @app.get(f"{API_PREFIX}/status")
    def status():
        up = STATE["proc"] is not None and STATE["proc"].poll() is None
        return jsonify({"internal_service": "healthy" if up else "down",
                        "protocol": RT.get("protocol"),
                        "error": STATE["error"]})

    @app.route(f"{API_PREFIX}/<path:sub>",
               methods=["GET", "POST", "PUT", "DELETE", "PATCH"])
    def proxy(sub):
        try:
            ensure_up()
        except Exception as exc:
            return jsonify({"error": f"internal service unavailable: {exc}"}), 503
        r = requests.request(request.method, f"{STATE['base']}/{sub}",
                             params=request.args, data=request.get_data(),
                             headers={"Content-Type": request.content_type or "",
                                      **_identity_headers()},
                             stream=True, timeout=120)
        drop = {"connection", "content-encoding", "content-length", "transfer-encoding"}
        headers = [(k, v) for k, v in r.headers.items() if k.lower() not in drop]
        return Response(r.iter_content(chunk_size=64 * 1024),
                        status=r.status_code, headers=headers)

    atexit.register(_shutdown)


def _shutdown():
    if STATE["proc"] and STATE["proc"].poll() is None:
        STATE["proc"].terminate()
```

安装与验证（对照 12.3）：解压 → 安装适配器依赖 → 校验 SHA256SUMS → 登记 `tools.json` → 重启 → 访问 `/api/hello-pro/status` 确认 `internal_service: healthy`。

> 内部服务（以任意语言实现）只需满足三件事即可被接入：监听回环随机端口并支持 `--port=` 参数、提供 `/healthz`、按 `jz-runtime-1` 契约处理请求与落盘数据——见 8.8 契约表。

---

*JZToolsHub · 插件设计规范 v1.1 · 一切皆插件 —— 源码与封装，皆可即插即用*
