# JSON 格式化（json-formatter）

纯前端 JSON 格式化与校验工具。

## 功能

- **格式化**：美化 JSON 输出，支持 2 空格 / 4 空格 / Tab 缩进
- **压缩**：去除所有空白，生成紧凑单行 JSON
- **校验**：语法错误时显示具体的错误位置与原因

## 特性

- 纯前端，全部处理在浏览器本地完成，不经过后端
- 校验失败时错误消息经 DOM API 安全渲染，防 XSS

## 依赖

无第三方依赖，纯浏览器标准 API。