# Base64 编解码（base64）

纯前端 Base64 编码与解码工具，支持中文 UTF-8。

## 功能

- **编码**：文本 → Base64，使用 `TextEncoder` 保证 UTF-8 中文正确编码
- **解码**：Base64 → 文本，非法输入提示错误
- **复制结果**：一键复制到剪贴板

## 特性

- 纯前端，全部处理在浏览器本地完成，不经过后端
- 支持 UTF-8 中文（非仅 ASCII）

## 依赖

无第三方依赖，纯浏览器标准 API。