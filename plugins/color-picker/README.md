# 取色器（color-picker）

纯前端颜色选择工具，实时显示并复制颜色值。

## 功能

- **颜色选择**：基于 `<input type="color">` 原生取色器
- **实时显示**：HEX / RGB / HSL 三种格式同步更新
- **一键复制**：每种格式独立复制按钮

## 特性

- 纯前端，不依赖任何后端服务
- 无第三方依赖，纯浏览器标准 API

## 已知限制

- 取色器为浏览器原生控件，不同浏览器取色面板外观可能不同