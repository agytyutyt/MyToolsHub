<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <!-- 唯一运行时权限：相机。禁止声明 INTERNET（完全离线，见文档 1.4） -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.InfoParse">

        <!-- 主页：历次识别结果（历史卡片） + 相机按钮入口（沉浸式系统栏） -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.InfoParse.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 识别页：相机实时扫码 / 图片导入 / 视频解析（沉浸式系统栏） -->
        <activity
            android:name=".ScanActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.InfoParse.Scan" />

        <activity
            android:name=".ResultActivity"
            android:exported="false"
            android:theme="@style/Theme.InfoParse.Result" />
    </application>

</manifest>
